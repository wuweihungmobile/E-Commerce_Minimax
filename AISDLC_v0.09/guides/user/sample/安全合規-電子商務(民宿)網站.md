# 安全與合規：民宿預訂電子商務網站

> **場景**: Security & Compliance - BnB Booking Platform 安全評估與合規檢查
> **技術棧**: React (Next.js) + Spring Boot + PostgreSQL + Google Cloud
> **AISDLC 版本**: v0.09
> **更新日期**: 2025-12-16

---

## 📋 目錄

1. [第一步：Cursor AI 專案路徑設定](#第一步cursor-ai-專案路徑設定)
2. [第二步：AISDLC 框架安裝](#第二步aisdlc-框架安裝)
3. [第三步：Claude Code 完整安全流程](#第三步claude-code-完整安全流程)
4. [附錄：安全工具與命令](#附錄安全工具與命令)

---

## 第一步：Cursor AI 專案路徑設定

### 1.1 分析現有專案結構

**假設現有專案結構**:
```
BnB_Booking_Platform/
├── frontend/                       # Next.js 前端
│   ├── app/
│   ├── components/
│   └── package.json
├── backend/                        # Spring Boot 後端
│   ├── src/main/java/com/bnb/
│   │   ├── controller/            # ⚠️ API Endpoints
│   │   ├── service/               # ⚠️ 業務邏輯
│   │   ├── repository/            # ⚠️ 資料存取
│   │   └── security/              # ⚠️ 安全配置（需補充）
│   └── pom.xml
└── docker-compose.yml
```

**安全隱憂**:
- ❌ SQL Injection 風險（未使用 Prepared Statements）
- ❌ XSS 攻擊風險（未消毒使用者輸入）
- ❌ CSRF 攻擊風險（無 CSRF Token）
- ❌ 身份驗證漏洞（弱密碼策略）
- ❌ 敏感資料外洩（信用卡資訊未加密）
- ❌ Session 劫持風險（Cookie 配置不安全）

### 1.2 建立安全評估結構

**在終端機執行**:
```bash
cd ~/Projects/BnB_Booking_Platform

# 建立安全文檔目錄
mkdir -p Docs/security/{assessment,vulnerabilities,compliance,policies}

# 建立安全測試目錄
mkdir -p security-tests/{static-analysis,dynamic-analysis,penetration}

# 驗證
tree -L 2 Docs/security security-tests
```

**完整專案結構**:
```
BnB_Booking_Platform/
├── frontend/
├── backend/
├── Docs/security/                  # 📄 安全文檔
│   ├── assessment/
│   ├── vulnerabilities/
│   ├── compliance/
│   └── policies/
├── security-tests/                 # 🔒 安全測試
│   ├── static-analysis/
│   ├── dynamic-analysis/
│   └── penetration/
└── AISDLC_v0.09/                  # 🔴 步驟 2 安裝
```

### 1.3 開啟 Cursor AI

1. **Cursor AI**: `File` → `Open Folder...`
2. 選擇 `~/Projects/BnB_Booking_Platform`

---

## 第二步：AISDLC 框架安裝

### 2.1 符號連結安裝

```bash
cd ~/Projects/BnB_Booking_Platform
ln -s /Users/wuweihong/Cursor_Project/AISDLC_ALL/AISDLC_v0.09 AISDLC_v0.09
ls -lah | grep AISDLC
```

### 2.2 驗證安裝

```bash
ls -la AISDLC_v0.09/AISDLC_INIT.md
cat AISDLC_v0.09/scenarios/security/SOP_QuickRef.md | head -50
```

---

## 第三步：Claude Code 完整安全流程

### 階段 1：安全需求分析（2-3 小時）

#### 3.1.1 啟動安全評估 Workflow

**在 Claude Code 輸入**:
```
請載入 AISDLC_INIT.md，我要進行「安全與合規」場景。

目標：民宿預訂電子商務網站安全評估與合規檢查

技術棧：
- 前端：React (Next.js 14) + TypeScript
- 後端：Spring Boot 3.x (Java 17) + PostgreSQL
- 部署：Google Cloud Platform
- 金流：第三方金流（Stripe/綠界）

安全隱憂：
1. SQL Injection 風險
2. XSS 攻擊風險
3. CSRF 攻擊風險
4. 身份驗證漏洞
5. 敏感資料外洩（信用卡、個資）
6. Session 劫持風險

合規需求：
- GDPR（個資保護）
- PCI-DSS（支付卡資料安全）
- 台灣個資法

請執行「security-assessment」workflow，生成安全評估計畫。
```

#### 3.1.2 生成威脅模型（STRIDE）

**在 Claude Code 輸入**:
```
請載入 Security Agent，生成威脅模型（STRIDE）：

資產識別：
1. 敏感資料
   - 使用者個資（姓名、Email、電話、地址）
   - 訂單資訊（房源、日期、金額）
   - 支付資訊（信用卡、交易記錄）
   - 房東資料（銀行帳號、身份證）

2. 系統元件
   - Next.js 前端（SSR）
   - Spring Boot API
   - PostgreSQL 資料庫
   - Redis Session Store
   - Google Cloud Storage（圖片）

威脅建模（STRIDE）：
- Spoofing：身份偽造、假冒訂單
- Tampering：資料篡改、訂單修改
- Repudiation：交易否認、無稽核
- Information Disclosure：個資外洩、SQL Injection
- Denial of Service：DDoS 攻擊、API 濫用
- Elevation of Privilege：權限提升、管理員劫持

請生成威脅模型文檔：`Docs/security/assessment/threat-model.md`
```

**預期輸出** (`threat-model.md`):
```markdown
# 民宿預訂平台威脅模型

## 資產清單

### 極高價值資產
1. **支付資訊**
   - 風險：信用卡資料外洩
   - 影響：法律責任、罰款（PCI-DSS）
   - 防護：第三方金流、不儲存卡號

2. **使用者個資**
   - 風險：GDPR 違規、個資法違反
   - 影響：高額罰款、信譽損失
   - 防護：加密、存取控制

3. **訂單資料**
   - 風險：交易資料篡改
   - 影響：財務損失、法律糾紛
   - 防護：Transaction 管理、稽核日誌

## STRIDE 威脅分析

### S - Spoofing（身份偽造）
| 威脅 | 現狀 | 風險 | 建議 |
|------|------|------|------|
| 假冒使用者身份 | 基本密碼驗證 | 🔴 Critical | JWT + Refresh Token + MFA |
| 假冒房東身份 | 無額外驗證 | 🔴 Critical | KYC 驗證、身份證上傳 |
| Session 劫持 | Cookie 配置不安全 | 🟡 High | HttpOnly + Secure + SameSite |

### T - Tampering（資料篡改）
| 威脅 | 現狀 | 風險 | 建議 |
|------|------|------|------|
| SQL Injection | 部分拼接 SQL | 🔴 Critical | Prepared Statements（JPA） |
| 訂單金額篡改 | 前端傳遞價格 | 🔴 Critical | 後端重新計算價格 |
| API 參數篡改 | 無簽章驗證 | 🟡 High | API 請求簽章 |

### R - Repudiation（否認操作）
| 威脅 | 現狀 | 風險 | 建議 |
|------|------|------|------|
| 交易否認 | 無完整日誌 | 🟡 Medium | 稽核日誌（AuditLog） |
| 操作記錄不全 | 僅 ERROR 日誌 | 🟢 Low | 記錄所有關鍵操作 |

### I - Information Disclosure（資訊洩漏）
| 威脅 | 現狀 | 風險 | 建議 |
|------|------|------|------|
| SQL Injection | 部分拼接 SQL | 🔴 Critical | Prepared Statements |
| XSS 攻擊 | 未消毒輸入 | 🔴 Critical | DOMPurify + CSP |
| 敏感資料外洩 | 明文儲存密碼 | 🔴 Critical | BCrypt + 加鹽 |
| API 過度回傳 | 回傳完整物件 | 🟡 High | DTO 模式、欄位過濾 |

### D - Denial of Service（阻斷服務）
| 威脅 | 現狀 | 風險 | 建議 |
|------|------|------|------|
| DDoS 攻擊 | 無防護 | 🟡 High | Google Cloud Armor |
| API 濫用 | 無速率限制 | 🟡 High | Rate Limiting（Bucket4j） |
| 資料庫連線耗盡 | 無連線池管理 | 🟡 Medium | HikariCP 配置 |

### E - Elevation of Privilege（權限提升）
| 威脅 | 現狀 | 風險 | 建議 |
|------|------|------|------|
| 橫向越權 | 僅檢查登入 | 🔴 Critical | RBAC + 資源擁有者檢查 |
| 縱向越權 | 無角色權限 | 🟡 High | Spring Security + @PreAuthorize |

## 優先修復清單

| 威脅 | 風險 | 優先級 | 預計工時 |
|------|------|--------|---------|
| SQL Injection | 🔴 Critical | P0 | 16 小時 |
| XSS 攻擊 | 🔴 Critical | P0 | 12 小時 |
| 橫向越權 | 🔴 Critical | P0 | 16 小時 |
| 明文儲存密碼 | 🔴 Critical | P0 | 8 小時 |
| CSRF 防護 | 🔴 Critical | P0 | 8 小時 |
| Session 劫持 | 🟡 High | P1 | 4 小時 |
| Rate Limiting | 🟡 High | P1 | 8 小時 |
```

---

### 階段 2：漏洞掃描（3-4 小時）

#### 3.2.1 前端靜態分析（ESLint + Security Plugins）

**在 Claude Code 輸入**:
```
請配置前端安全掃描工具：

工具：
1. ESLint + eslint-plugin-security
2. next-safe（Next.js 安全檢查）
3. npm audit（依賴漏洞）

請更新 `frontend/package.json` 和 `frontend/.eslintrc.js`
```

**Claude Code 生成** (`frontend/.eslintrc.js`):
```javascript
module.exports = {
  extends: [
    'next/core-web-vitals',
    'plugin:security/recommended'  // ✅ 安全規則
  ],
  plugins: ['security'],
  rules: {
    'security/detect-object-injection': 'error',
    'security/detect-non-literal-regexp': 'error',
    'security/detect-unsafe-regex': 'error',
    'security/detect-buffer-noassert': 'error',
    'security/detect-eval-with-expression': 'error',
    'security/detect-no-csrf-before-method-override': 'error',
    'security/detect-possible-timing-attacks': 'error'
  }
}
```

**執行掃描**:
```bash
cd frontend
npm install eslint-plugin-security --save-dev
npm audit
npx eslint . --ext .js,.jsx,.ts,.tsx
```

#### 3.2.2 後端靜態分析（SpotBugs + OWASP）

**在 Claude Code 輸入**:
```
請配置後端安全掃描工具：

工具：
1. SpotBugs（靜態分析）
2. OWASP Dependency-Check（依賴掃描）
3. SonarQube（程式碼品質）

請更新 `backend/pom.xml`
```

**Claude Code 生成** (`backend/pom.xml`):
```xml
<build>
    <plugins>
        <!-- SpotBugs -->
        <plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <version>4.8.2.0</version>
            <configuration>
                <effort>Max</effort>
                <threshold>Low</threshold>
                <includeFilterFile>spotbugs-security-include.xml</includeFilterFile>
            </configuration>
        </plugin>

        <!-- OWASP Dependency-Check -->
        <plugin>
            <groupId>org.owasp</groupId>
            <artifactId>dependency-check-maven</artifactId>
            <version>9.0.7</version>
            <executions>
                <execution>
                    <goals>
                        <goal>check</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
```

**執行掃描**:
```bash
cd backend
mvn spotbugs:check
mvn org.owasp:dependency-check-maven:check
```

---

### 階段 3：安全修復實施（2-3 天）

#### 3.3.1 SQL Injection 防護（JPA + Prepared Statements）

**在 Claude Code 輸入**:
```
請修復 SQL Injection 漏洞：

問題：BookingRepository 使用原生 SQL 拼接
風險：🔴 Critical

請修改：`backend/src/main/java/com/bnb/repository/BookingRepository.java`
使用 JPA Query Methods 或 @Query with Parameters
```

**Claude Code 生成** (`BookingRepository.java`):
```java
package com.bnb.repository;

import com.bnb.entity.Booking;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import java.time.LocalDate;
import java.util.List;

public interface BookingRepository extends JpaRepository<Booking, Long> {

    // ✅ 安全寫法：JPA Query Method
    List<Booking> findByUserId(Long userId);

    // ✅ 安全寫法：@Query with Named Parameters
    @Query("SELECT b FROM Booking b WHERE b.checkIn >= :startDate AND b.checkOut <= :endDate")
    List<Booking> findBookingsByDateRange(
        @Param("startDate") LocalDate startDate,
        @Param("endDate") LocalDate endDate
    );

    // ✅ 安全寫法：Native Query with Parameters
    @Query(value = "SELECT * FROM bookings WHERE room_id = :roomId AND status = :status",
           nativeQuery = true)
    List<Booking> findByRoomIdAndStatus(
        @Param("roomId") Long roomId,
        @Param("status") String status
    );

    // ❌ 危險寫法（已移除）：
    // @Query(value = "SELECT * FROM bookings WHERE room_id = " + roomId, nativeQuery = true)
}
```

#### 3.3.2 XSS 防護（前端 + 後端）

**在 Claude Code 輸入**:
```
請修復 XSS 漏洞：

前端：
1. 使用 DOMPurify 消毒使用者輸入
2. 設定 Content Security Policy (CSP)

後端：
1. 使用 OWASP Java Encoder
2. 驗證所有輸入

請修改：
- `frontend/components/RoomCard.tsx`
- `backend/src/main/java/com/bnb/controller/RoomController.java`
```

**前端防護** (`frontend/components/RoomCard.tsx`):
```typescript
import DOMPurify from 'isomorphic-dompurify'

export default function RoomCard({ room }: { room: Room }) {
  // ✅ 安全寫法：消毒 HTML
  const sanitizedDescription = DOMPurify.sanitize(room.description)

  return (
    <div className="room-card">
      <h2>{room.title}</h2>
      {/* ✅ 使用 dangerouslySetInnerHTML 前必須消毒 */}
      <div dangerouslySetInnerHTML={{ __html: sanitizedDescription }} />

      {/* ❌ 危險寫法（已移除）：
      <div dangerouslySetInnerHTML={{ __html: room.description }} />
      */}
    </div>
  )
}
```

**CSP 配置** (`frontend/next.config.js`):
```javascript
module.exports = {
  async headers() {
    return [
      {
        source: '/:path*',
        headers: [
          {
            key: 'Content-Security-Policy',
            value: `
              default-src 'self';
              script-src 'self' 'unsafe-eval' 'unsafe-inline';
              style-src 'self' 'unsafe-inline';
              img-src 'self' data: https:;
              font-src 'self' data:;
              connect-src 'self' https://api.example.com;
            `.replace(/\s{2,}/g, ' ').trim()
          }
        ]
      }
    ]
  }
}
```

**後端驗證** (`backend/RoomController.java`):
```java
package com.bnb.controller;

import com.bnb.dto.CreateRoomRequest;
import org.owasp.encoder.Encode;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;
import javax.validation.Valid;
import javax.validation.constraints.*;

@RestController
@RequestMapping("/api/rooms")
@Validated
public class RoomController {

    @PostMapping
    public ResponseEntity<Room> createRoom(@Valid @RequestBody CreateRoomRequest request) {
        // ✅ 使用 OWASP Encoder 消毒輸出
        String safeTitle = Encode.forHtml(request.getTitle());
        String safeDescription = Encode.forHtml(request.getDescription());

        // ✅ DTO 驗證（Bean Validation）
        // @NotBlank, @Size, @Pattern 已在 DTO 定義

        Room room = Room.builder()
            .title(safeTitle)
            .description(safeDescription)
            .build();

        return ResponseEntity.ok(roomService.create(room));
    }
}
```

**DTO 驗證** (`CreateRoomRequest.java`):
```java
package com.bnb.dto;

import javax.validation.constraints.*;

public class CreateRoomRequest {

    @NotBlank(message = "房源標題不可為空")
    @Size(min = 5, max = 100, message = "標題長度必須在 5-100 字元")
    @Pattern(regexp = "^[\\p{L}\\p{N}\\s.,!?-]+$", message = "標題包含非法字元")
    private String title;

    @NotBlank(message = "房源描述不可為空")
    @Size(max = 2000, message = "描述不可超過 2000 字元")
    private String description;

    @NotNull(message = "價格不可為空")
    @Min(value = 100, message = "價格不可低於 100")
    @Max(value = 100000, message = "價格不可超過 100000")
    private Integer pricePerNight;

    // Getters and Setters...
}
```

#### 3.3.3 CSRF 防護（Spring Security）

**在 Claude Code 輸入**:
```
請實施 CSRF 防護：

功能：
1. Spring Security CSRF Token
2. SameSite Cookie 配置
3. 前端自動帶入 CSRF Token

請修改：`backend/src/main/java/com/bnb/config/SecurityConfig.java`
```

**Claude Code 生成** (`SecurityConfig.java`):
```java
package com.bnb.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.web.SecurityFilterChain;
import org.springframework.security.web.csrf.CookieCsrfTokenRepository;

@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            // ✅ 啟用 CSRF 防護
            .csrf(csrf -> csrf
                .csrfTokenRepository(CookieCsrfTokenRepository.withHttpOnlyFalse())
                .ignoringRequestMatchers("/api/public/**")  // 公開 API 排除
            )

            // ✅ Cookie 安全配置
            .sessionManagement(session -> session
                .sessionFixation().migrateSession()
            )

            // ✅ 安全 Headers
            .headers(headers -> headers
                .contentSecurityPolicy(csp ->
                    csp.policyDirectives("default-src 'self'"))
                .frameOptions(frame -> frame.deny())
                .xssProtection(xss -> xss.block(true))
            )

            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/public/**").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            );

        return http.build();
    }
}
```

#### 3.3.4 權限控制（RBAC + 資源擁有者檢查）

**在 Claude Code 輸入**:
```
請實施細粒度權限控制：

問題：橫向越權（使用者可修改他人訂單）
風險：🔴 Critical

請修改：`backend/src/main/java/com/bnb/service/BookingService.java`
實施：RBAC + 資源擁有者檢查
```

**Claude Code 生成** (`BookingService.java`):
```java
package com.bnb.service;

import com.bnb.entity.Booking;
import com.bnb.exception.UnauthorizedException;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.security.core.Authentication;
import org.springframework.stereotype.Service;

@Service
public class BookingService {

    // ✅ 方法級權限檢查
    @PreAuthorize("hasRole('USER')")
    public Booking createBooking(CreateBookingRequest request, Authentication auth) {
        Long userId = getUserIdFromAuth(auth);

        Booking booking = Booking.builder()
            .userId(userId)
            .roomId(request.getRoomId())
            .checkIn(request.getCheckIn())
            .checkOut(request.getCheckOut())
            .build();

        return bookingRepository.save(booking);
    }

    // ✅ 資源擁有者檢查
    @PreAuthorize("hasRole('USER')")
    public Booking updateBooking(Long bookingId, UpdateBookingRequest request, Authentication auth) {
        Booking booking = bookingRepository.findById(bookingId)
            .orElseThrow(() -> new NotFoundException("訂單不存在"));

        Long userId = getUserIdFromAuth(auth);

        // ✅ 檢查資源擁有者
        if (!booking.getUserId().equals(userId)) {
            throw new UnauthorizedException("您無權修改此訂單");
        }

        booking.setCheckIn(request.getCheckIn());
        booking.setCheckOut(request.getCheckOut());

        return bookingRepository.save(booking);
    }

    // ✅ 管理員可查看所有訂單
    @PreAuthorize("hasRole('ADMIN')")
    public List<Booking> getAllBookings() {
        return bookingRepository.findAll();
    }

    private Long getUserIdFromAuth(Authentication auth) {
        UserDetails userDetails = (UserDetails) auth.getPrincipal();
        return userDetails.getUserId();
    }
}
```

#### 3.3.5 密碼加密（BCrypt）

**在 Claude Code 輸入**:
```
請實施密碼加密：

問題：明文儲存密碼
風險：🔴 Critical

請修改：`backend/src/main/java/com/bnb/service/UserService.java`
使用 BCrypt + 加鹽
```

**Claude Code 生成** (`UserService.java`):
```java
package com.bnb.service;

import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.stereotype.Service;

@Service
public class UserService {

    private final BCryptPasswordEncoder passwordEncoder = new BCryptPasswordEncoder(12);  // ✅ 12 rounds

    public User register(RegisterRequest request) {
        // ✅ 密碼強度驗證
        validatePasswordStrength(request.getPassword());

        // ✅ BCrypt 加密（自動加鹽）
        String hashedPassword = passwordEncoder.encode(request.getPassword());

        User user = User.builder()
            .email(request.getEmail())
            .password(hashedPassword)  // ✅ 儲存加密後的密碼
            .build();

        return userRepository.save(user);
    }

    public boolean authenticate(String email, String rawPassword) {
        User user = userRepository.findByEmail(email)
            .orElseThrow(() -> new NotFoundException("使用者不存在"));

        // ✅ BCrypt 驗證
        return passwordEncoder.matches(rawPassword, user.getPassword());
    }

    private void validatePasswordStrength(String password) {
        // ✅ 密碼強度規則：8+ 字元、大小寫、數字、特殊字元
        String passwordRegex = "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]{8,}$";
        if (!password.matches(passwordRegex)) {
            throw new ValidationException("密碼必須包含至少 8 字元、大小寫字母、數字和特殊字元");
        }
    }
}
```

---

### 階段 4：合規性檢查（0.5-1 天）

#### 3.4.1 GDPR 合規實施

**在 Claude Code 輸入**:
```
請實施 GDPR 合規功能：

功能：
1. Cookie 同意橫幅
2. 使用者資料匯出（Right to Access）
3. 使用者資料刪除（Right to be Forgotten）
4. 隱私政策頁面

請生成：
- 前端：`frontend/components/CookieConsent.tsx`
- 後端：`backend/src/main/java/com/bnb/controller/GdprController.java`
```

**前端 Cookie 同意** (`CookieConsent.tsx`):
```typescript
'use client'

import { useState, useEffect } from 'react'
import Cookies from 'js-cookie'

export default function CookieConsent() {
  const [showBanner, setShowBanner] = useState(false)

  useEffect(() => {
    const consent = Cookies.get('cookie-consent')
    if (!consent) {
      setShowBanner(true)
    }
  }, [])

  const acceptCookies = () => {
    Cookies.set('cookie-consent', 'accepted', { expires: 365, sameSite: 'strict' })
    setShowBanner(false)
  }

  if (!showBanner) return null

  return (
    <div className="fixed bottom-0 left-0 right-0 bg-gray-900 text-white p-4 z-50">
      <div className="container mx-auto flex justify-between items-center">
        <p>
          我們使用 Cookie 來改善您的使用體驗。繼續使用本網站即表示您同意我們的{' '}
          <a href="/privacy-policy" className="underline">隱私政策</a>。
        </p>
        <button onClick={acceptCookies} className="bg-blue-600 px-6 py-2 rounded">
          接受
        </button>
      </div>
    </div>
  )
}
```

**後端 GDPR API** (`GdprController.java`):
```java
package com.bnb.controller;

import com.bnb.service.GdprService;
import org.springframework.security.core.Authentication;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/api/gdpr")
public class GdprController {

    private final GdprService gdprService;

    // ✅ Right to Access：匯出使用者資料
    @GetMapping("/export-data")
    public ResponseEntity<byte[]> exportUserData(Authentication auth) {
        Long userId = getUserIdFromAuth(auth);

        // 匯出為 JSON
        byte[] jsonData = gdprService.exportUserData(userId);

        return ResponseEntity.ok()
            .header("Content-Disposition", "attachment; filename=user-data.json")
            .contentType(MediaType.APPLICATION_JSON)
            .body(jsonData);
    }

    // ✅ Right to be Forgotten：刪除使用者資料
    @DeleteMapping("/delete-account")
    public ResponseEntity<Void> deleteAccount(
        @RequestParam String confirmationToken,
        Authentication auth
    ) {
        Long userId = getUserIdFromAuth(auth);

        // 二次確認（需先發送確認 Email）
        gdprService.deleteUserAccount(userId, confirmationToken);

        return ResponseEntity.noContent().build();
    }
}
```

#### 3.4.2 PCI-DSS 合規（支付卡資料安全）

**在 Claude Code 輸入**:
```
請確認 PCI-DSS 合規：

檢查項目：
1. 不儲存信用卡卡號（CVV/CVC）
2. 使用第三方金流（Stripe/綠界）
3. HTTPS 強制加密
4. 稽核日誌

請生成合規報告：`Docs/security/compliance/pci-dss-compliance.md`
```

**預期輸出** (`pci-dss-compliance.md`):
```markdown
# PCI-DSS 合規性檢查報告

## 合規狀態總覽

| 要求 | 狀態 | 實施方式 |
|------|------|---------|
| 不儲存卡號/CVV | ✅ 已實施 | 使用 Stripe 金流 |
| 傳輸加密 | ✅ 已實施 | HTTPS (TLS 1.3) |
| 存取控制 | ✅ 已實施 | RBAC + 最小權限 |
| 網路安全 | ✅ 已實施 | Google Cloud Armor |
| 稽核日誌 | ✅ 已實施 | Spring AOP Logging |
| 安全測試 | ✅ 已實施 | OWASP ZAP 掃描 |

## 關鍵實施

### 1. 第三方金流整合（Stripe）
- **不儲存卡號**: 所有支付資訊由 Stripe 處理
- **PCI 認證**: Stripe 已獲得 PCI Level 1 認證
- **Token 化**: 使用 Stripe Token，不碰觸敏感資料

### 2. HTTPS 強制加密
- **TLS 版本**: TLS 1.3
- **憑證**: Google-managed SSL
- **HSTS**: 強制 HTTPS（max-age=31536000）

### 3. 稽核日誌
- **記錄內容**: 使用者 ID、操作、時間、IP
- **保留期限**: 1 年
- **存取權限**: 僅管理員可查看
```

---

### 階段 5：安全測試（1 天）

#### 3.5.1 OWASP ZAP 動態掃描

**執行掃描**:
```bash
# 安裝 OWASP ZAP
brew install --cask owasp-zap

# 啟動本地環境
docker-compose up -d

# 執行 ZAP 掃描
zap-cli quick-scan --self-contained \
  --start-options '-config api.disablekey=true' \
  http://localhost:3000

# 生成報告
zap-cli report -o security-tests/dynamic-analysis/zap-report.html -f html
```

---

## 附錄：安全工具與命令

### 前端安全檢查

```bash
# ESLint 安全掃描
cd frontend
npx eslint . --ext .js,.jsx,.ts,.tsx

# 依賴漏洞掃描
npm audit

# Next.js 安全檢查
npx next-safe
```

### 後端安全檢查

```bash
# SpotBugs
cd backend
mvn spotbugs:check

# OWASP Dependency-Check
mvn org.owasp:dependency-check-maven:check

# SonarQube
mvn sonar:sonar
```

### 滲透測試

```bash
# OWASP ZAP
zap-cli quick-scan http://localhost:3000

# Nikto Web 掃描
nikto -h http://localhost:3000
```

---

## 🎯 安全時程表（總計 4-6 天）

| 階段 | 天數 | 主要工作 |
|------|-----|---------|
| 1. 安全需求分析 | 0.5 天 | 威脅模型（STRIDE）|
| 2. 漏洞掃描 | 0.5 天 | 靜態/動態分析 |
| 3. 安全修復 | 2-3 天 | SQL Injection、XSS、CSRF、權限控制 |
| 4. 合規性檢查 | 0.5-1 天 | GDPR、PCI-DSS |
| 5. 安全測試 | 1 天 | OWASP ZAP、滲透測試 |
| 6. 最終報告 | 0.5 天 | 安全評估總結 |

---

## 📚 相關文檔

- [AISDLC_INIT.md](../../AISDLC_INIT.md)
- [Security SOP](../../scenarios/security/SOP.md)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [GDPR Compliance](https://gdpr.eu/)
- [PCI-DSS](https://www.pcisecuritystandards.org/)

---

**版本**: 1.0
**作者**: AISDLC Framework Team
**最後更新**: 2025-12-16
