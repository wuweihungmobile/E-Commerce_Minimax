# 效能調校：電子商務網站（民宿預訂平台）

> **場景**: Performance Optimization - 全面提升民宿網站效能（前後端 + 資料庫）
> **技術棧**: Next.js 14 + Spring Boot 3.2 + PostgreSQL + Google Cloud
> **AISDLC 版本**: v0.09
> **更新日期**: 2025-12-15

---

## 📋 目錄

1. [第一步：Cursor AI 專案路徑設定](#第一步cursor-ai-專案路徑設定)
2. [第二步：AISDLC 框架安裝](#第二步aisdlc-框架安裝)
3. [第三步：Claude Code 完整效能調校流程](#第三步claude-code-完整效能調校流程)
4. [附錄：命令速查表](#附錄命令速查表)

---

## 第一步：Cursor AI 專案路徑設定

### 1.1 分析現有專案結構

**假設現有專案結構**:
```
BnB-Platform/
├── frontend/                     # Next.js 13 (Pages Router)
│   ├── pages/
│   ├── components/
│   ├── styles/
│   └── package.json
├── backend/                      # Spring Boot 3.2
│   ├── src/main/java/com/bnb/
│   │   ├── controller/
│   │   ├── service/
│   │   ├── repository/
│   │   └── model/
│   ├── pom.xml
│   └── application.yml
├── database/
│   └── init.sql
└── docker-compose.yml
```

**現有效能問題診斷**:
- ❌ 首頁載入時間 > 5 秒
- ❌ 房源搜尋 API 回應 > 2 秒
- ❌ 圖片載入慢（無 CDN、無優化）
- ❌ 資料庫查詢慢（N+1 問題、無索引）
- ❌ 無快取機制（Redis）
- ❌ 前端 Bundle 體積過大（1.5 MB）
- ❌ Lighthouse 分數低（Performance: 45/100）

### 1.2 建立效能調校專案結構

**在終端機執行**:
```bash
cd ~/Projects/BnB-Platform

# 建立效能分析目錄
mkdir -p performance-analysis
mkdir -p performance-analysis/lighthouse        # Lighthouse 報告
mkdir -p performance-analysis/profiling         # 效能分析
mkdir -p performance-analysis/database          # 資料庫慢查詢日誌
mkdir -p performance-analysis/baseline          # 基準測試

# 建立文檔目錄
mkdir -p Docs
mkdir -p Docs/performance                       # 效能調校文檔
mkdir -p Docs/optimization                      # 優化方案
mkdir -p Docs/reports                           # 驗證報告

# 驗證
tree -L 2 -d
```

**完整專案結構**:
```
BnB-Platform/
├── frontend/                     # 🎯 Next.js 專案
│   ├── pages/
│   └── package.json
├── backend/                      # 🎯 Spring Boot 專案
│   ├── src/
│   └── pom.xml
├── performance-analysis/         # 📊 效能分析資料
│   ├── lighthouse/
│   ├── profiling/
│   ├── database/
│   └── baseline/
├── Docs/                         # 📄 AISDLC 文檔
│   ├── performance/
│   ├── optimization/
│   └── reports/
└── AISDLC_v0.09/                # 🔴 步驟 2 安裝
```

### 1.3 開啟 Cursor AI

**步驟**:
1. 打開 Cursor 應用程式
2. `File` → `Open Folder...`
3. 選擇：`~/Projects/BnB-Platform` （**專案根目錄**）
4. 點擊「Open」

**驗證**: Cursor 左側應顯示 `frontend/`, `backend/`, `performance-analysis/`, `Docs/`

---

## 第二步：AISDLC 框架安裝

### 2.1 方法一：符號連結（推薦）

```bash
cd ~/Projects/BnB-Platform

# 建立符號連結
ln -s /Users/你的用戶名/Cursor_Project/AISDLC_ALL/AISDLC_v0.09 AISDLC_v0.09

# 驗證
ls -lah | grep AISDLC
```

### 2.2 方法二：複製

```bash
cp -r /Users/你的用戶名/Cursor_Project/AISDLC_ALL/AISDLC_v0.09 ~/Projects/BnB-Platform/

# 驗證
ls AISDLC_v0.09/
```

### 2.3 驗證安裝

```bash
ls -la AISDLC_v0.09/AISDLC_INIT.md
cat AISDLC_v0.09/scenarios/performance/README.md | head -30
```

---

## 第三步：Claude Code 完整效能調校流程

### 階段 1：效能基準測試與問題診斷（2 週）

#### 3.1.1 啟動效能分析 Workflow

**在 Claude Code 輸入**:
```
請載入 AISDLC_INIT.md，我要進行「效能調校」場景。

目標：民宿預訂平台全面效能優化

技術棧：
- 前端：Next.js 13 (Pages Router) → Next.js 14 (App Router)
- 後端：Spring Boot 3.2
- 資料庫：PostgreSQL 15
- 部署：Google Cloud (GKE + Cloud SQL)

目前問題：
1. 首頁載入 > 5 秒
2. 房源搜尋 API > 2 秒
3. 圖片載入慢
4. 資料庫慢查詢（N+1 問題）
5. 無快取機制
6. Bundle 體積 1.5 MB
7. Lighthouse 分數 45/100

請執行「performance-profiling-and-analysis」workflow，進行全面效能分析。
```

#### 3.1.2 前端效能測試（Lighthouse）

**在 Claude Code 輸入**:
```
請指導我執行 Lighthouse 效能測試：

測試頁面：
1. 首頁 (/)
2. 房源列表 (/properties)
3. 房源詳情 (/properties/[id])
4. 訂房頁面 (/booking)

請生成測試腳本：`performance-analysis/lighthouse/run-lighthouse.sh`
```

**Claude Code 生成** (`performance-analysis/lighthouse/run-lighthouse.sh`):
```bash
#!/bin/bash

# Lighthouse 自動化測試腳本
# 需求: npm install -g lighthouse

OUTPUT_DIR="performance-analysis/lighthouse/reports"
mkdir -p $OUTPUT_DIR

BASE_URL="http://localhost:3000"

echo "🚀 開始 Lighthouse 效能測試..."

# 1. 首頁
echo "📊 測試首頁..."
lighthouse $BASE_URL \
  --output=html \
  --output=json \
  --output-path="${OUTPUT_DIR}/homepage" \
  --chrome-flags="--headless" \
  --only-categories=performance,accessibility,best-practices,seo

# 2. 房源列表
echo "📊 測試房源列表..."
lighthouse "${BASE_URL}/properties" \
  --output=html \
  --output=json \
  --output-path="${OUTPUT_DIR}/property-list" \
  --chrome-flags="--headless" \
  --only-categories=performance

# 3. 房源詳情
echo "📊 測試房源詳情..."
lighthouse "${BASE_URL}/properties/12345" \
  --output=html \
  --output=json \
  --output-path="${OUTPUT_DIR}/property-detail" \
  --chrome-flags="--headless" \
  --only-categories=performance

# 4. 訂房頁面
echo "📊 測試訂房頁面..."
lighthouse "${BASE_URL}/booking?propertyId=12345" \
  --output=html \
  --output=json \
  --output-path="${OUTPUT_DIR}/booking-page" \
  --chrome-flags="--headless" \
  --only-categories=performance

echo "✅ Lighthouse 測試完成！報告位置: ${OUTPUT_DIR}"
```

**執行測試**:
```bash
# 啟動開發伺服器
cd frontend
npm run dev

# 執行 Lighthouse（另開終端機）
cd ~/Projects/BnB-Platform
chmod +x performance-analysis/lighthouse/run-lighthouse.sh
./performance-analysis/lighthouse/run-lighthouse.sh
```

**預期結果** (`performance-analysis/lighthouse/reports/homepage.report.json`):
```json
{
  "categories": {
    "performance": {
      "score": 0.45,  // 🔴 45/100
      "title": "Performance"
    }
  },
  "audits": {
    "first-contentful-paint": {
      "score": 0.34,
      "displayValue": "3.2 s",  // 🔴 目標 < 1.8s
      "numericValue": 3200
    },
    "largest-contentful-paint": {
      "score": 0.28,
      "displayValue": "5.8 s",  // 🔴 目標 < 2.5s
      "numericValue": 5800
    },
    "total-blocking-time": {
      "score": 0.41,
      "displayValue": "850 ms",  // 🔴 目標 < 200ms
      "numericValue": 850
    },
    "cumulative-layout-shift": {
      "score": 0.67,
      "displayValue": "0.18",  // ⚠️  目標 < 0.1
      "numericValue": 0.18
    },
    "speed-index": {
      "score": 0.39,
      "displayValue": "4.5 s",  // 🔴 目標 < 3.4s
      "numericValue": 4500
    }
  }
}
```

#### 3.1.3 後端效能測試（JMeter）

**在 Claude Code 輸入**:
```
請生成 JMeter 測試計畫，測試後端 API 效能：

測試 API：
1. GET /api/properties（房源搜尋）
2. GET /api/properties/{id}（房源詳情）
3. POST /api/bookings（建立訂單）
4. GET /api/bookings/{id}（訂單查詢）

併發：100 使用者
持續時間：5 分鐘

請生成 JMeter 腳本：`performance-analysis/profiling/api-load-test.jmx`
```

**Claude Code 生成** (`performance-analysis/profiling/api-load-test.jmx`):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<jmeterTestPlan version="1.2">
  <hashTree>
    <TestPlan guiclass="TestPlanGui" testclass="TestPlan" testname="BnB Platform API Load Test">
      <elementProp name="TestPlan.user_defined_variables" elementType="Arguments">
        <collectionProp name="Arguments.arguments">
          <elementProp name="BASE_URL" elementType="Argument">
            <stringProp name="Argument.name">BASE_URL</stringProp>
            <stringProp name="Argument.value">http://localhost:8080</stringProp>
          </elementProp>
        </collectionProp>
      </elementProp>
    </TestPlan>
    <hashTree>
      <!-- Thread Group: 100 併發使用者 -->
      <ThreadGroup guiclass="ThreadGroupGui" testclass="ThreadGroup" testname="API Users">
        <stringProp name="ThreadGroup.num_threads">100</stringProp>
        <stringProp name="ThreadGroup.ramp_time">30</stringProp>
        <stringProp name="ThreadGroup.duration">300</stringProp>
        <boolProp name="ThreadGroup.scheduler">true</boolProp>
      </ThreadGroup>
      <hashTree>
        <!-- HTTP Request: 房源搜尋 -->
        <HTTPSamplerProxy guiclass="HttpTestSampleGui" testclass="HTTPSamplerProxy" testname="GET /api/properties">
          <stringProp name="HTTPSampler.domain">${BASE_URL}</stringProp>
          <stringProp name="HTTPSampler.path">/api/properties?city=台北&maxPrice=5000</stringProp>
          <stringProp name="HTTPSampler.method">GET</stringProp>
        </HTTPSamplerProxy>

        <!-- HTTP Request: 房源詳情 -->
        <HTTPSamplerProxy guiclass="HttpTestSampleGui" testclass="HTTPSamplerProxy" testname="GET /api/properties/{id}">
          <stringProp name="HTTPSampler.domain">${BASE_URL}</stringProp>
          <stringProp name="HTTPSampler.path">/api/properties/${__Random(1,1000)}</stringProp>
          <stringProp name="HTTPSampler.method">GET</stringProp>
        </HTTPSamplerProxy>

        <!-- Listener: 彙總報告 -->
        <ResultCollector guiclass="SummaryReport" testclass="ResultCollector" testname="Summary Report">
          <boolProp name="ResultCollector.error_logging">false</boolProp>
          <objProp>
            <name>saveConfig</name>
            <value class="SampleSaveConfiguration">
              <time>true</time>
              <latency>true</latency>
              <timestamp>true</timestamp>
              <success>true</success>
              <label>true</label>
              <code>true</code>
              <message>true</message>
              <threadName>true</threadName>
              <dataType>true</dataType>
              <encoding>false</encoding>
              <assertions>true</assertions>
              <subresults>true</subresults>
              <responseData>false</responseData>
              <samplerData>false</samplerData>
              <xml>false</xml>
              <fieldNames>true</fieldNames>
              <responseHeaders>false</responseHeaders>
              <requestHeaders>false</requestHeaders>
              <responseDataOnError>false</responseDataOnError>
              <saveAssertionResultsFailureMessage>true</saveAssertionResultsFailureMessage>
              <assertionsResultsToSave>0</assertionsResultsToSave>
              <bytes>true</bytes>
              <sentBytes>true</sentBytes>
              <url>true</url>
              <threadCounts>true</threadCounts>
              <idleTime>true</idleTime>
              <connectTime>true</connectTime>
            </value>
          </objProp>
          <stringProp name="filename">performance-analysis/profiling/jmeter-results.csv</stringProp>
        </ResultCollector>
      </hashTree>
    </hashTree>
  </hashTree>
</jmeterTestPlan>
```

**執行測試**:
```bash
# 安裝 JMeter
brew install jmeter  # macOS
# 或下載: https://jmeter.apache.org/download_jmeter.cgi

# 執行測試
jmeter -n -t performance-analysis/profiling/api-load-test.jmx \
  -l performance-analysis/profiling/jmeter-results.csv \
  -e -o performance-analysis/profiling/jmeter-report

# 查看報告
open performance-analysis/profiling/jmeter-report/index.html
```

**預期結果**:
```
GET /api/properties
- 樣本數: 5,000
- 平均回應時間: 2,345 ms  🔴 目標 < 500ms
- P95 回應時間: 4,567 ms  🔴 目標 < 1000ms
- 吞吐量: 42.5 req/sec
- 錯誤率: 0.2%

GET /api/properties/{id}
- 平均回應時間: 1,234 ms  🔴 目標 < 300ms
- P95 回應時間: 2,345 ms  🔴 目標 < 600ms
```

#### 3.1.4 資料庫慢查詢分析

**在 Claude Code 輸入**:
```
請啟用 PostgreSQL 慢查詢日誌，分析資料庫效能問題：

1. 修改 postgresql.conf 啟用慢查詢日誌
2. 分析慢查詢（查詢時間 > 100ms）
3. 識別 N+1 查詢問題
4. 識別缺少索引的查詢

請生成配置和分析腳本。
```

**Claude Code 生成**:

**1. PostgreSQL 配置** (`database/postgresql.conf`):
```conf
# 啟用慢查詢日誌
log_min_duration_statement = 100  # 記錄超過 100ms 的查詢
log_line_prefix = '%t [%p]: [%l-1] user=%u,db=%d,app=%a,client=%h '
log_statement = 'all'
log_duration = on

# 查詢統計
shared_preload_libraries = 'pg_stat_statements'
pg_stat_statements.track = all
```

**2. 慢查詢分析腳本** (`database/analyze-slow-queries.sql`):
```sql
-- 啟用 pg_stat_statements
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;

-- 查詢最慢的 20 個查詢
SELECT
    query,
    calls,
    total_exec_time,
    mean_exec_time,
    max_exec_time,
    stddev_exec_time,
    rows
FROM pg_stat_statements
WHERE mean_exec_time > 100  -- 平均超過 100ms
ORDER BY mean_exec_time DESC
LIMIT 20;

-- 查詢 N+1 問題（高呼叫次數但回傳少量資料）
SELECT
    query,
    calls,
    mean_exec_time,
    rows / calls AS avg_rows_per_call
FROM pg_stat_statements
WHERE calls > 100
  AND (rows / calls) < 10  -- 每次查詢平均回傳少於 10 行
ORDER BY calls DESC
LIMIT 20;

-- 查詢缺少索引的表格（Sequential Scan）
SELECT
    schemaname,
    tablename,
    seq_scan,
    seq_tup_read,
    idx_scan,
    seq_tup_read / seq_scan AS avg_seq_tup_read
FROM pg_stat_user_tables
WHERE seq_scan > 0
ORDER BY seq_tup_read DESC
LIMIT 20;
```

**執行分析**:
```bash
# 連接資料庫
psql -h localhost -U postgres -d bnb_platform

# 執行分析
\i database/analyze-slow-queries.sql

# 輸出結果到檔案
psql -h localhost -U postgres -d bnb_platform -f database/analyze-slow-queries.sql \
  > performance-analysis/database/slow-queries-report.txt
```

**預期發現** (`performance-analysis/database/slow-queries-report.txt`):
```
慢查詢 Top 5:

1. SELECT * FROM properties WHERE city = $1 ORDER BY created_at DESC
   - 呼叫次數: 5,234
   - 平均耗時: 1,234 ms  🔴
   - 問題: 缺少 city 索引，Full Table Scan

2. SELECT * FROM property_images WHERE property_id = $1
   - 呼叫次數: 10,567  🔴 N+1 問題！
   - 平均耗時: 45 ms
   - 問題: 在迴圈中逐筆查詢

3. SELECT * FROM bookings WHERE user_id = $1 AND status = 'CONFIRMED'
   - 呼叫次數: 3,456
   - 平均耗時: 567 ms  🔴
   - 問題: 缺少 (user_id, status) 複合索引

4. SELECT COUNT(*) FROM reviews WHERE property_id = $1
   - 呼叫次數: 8,901
   - 平均耗時: 234 ms  ⚠️
   - 問題: 每次都重新計算，應使用快取

5. SELECT * FROM users WHERE email = $1
   - 呼叫次數: 2,345
   - 平均耗時: 123 ms  ⚠️
   - 問題: email 欄位無索引
```

#### 3.1.5 生成效能診斷報告

**在 Claude Code 輸入**:
```
請載入 QA Agent (Quincy)，分析所有效能數據：

1. Lighthouse 報告
2. JMeter 壓測結果
3. 資料庫慢查詢分析

請生成綜合診斷報告：`Docs/performance/performance-diagnosis-report.md`
```

**預期輸出摘要**:
```markdown
# 效能診斷報告

## 🔴 嚴重問題（Critical）

### 1. 首頁載入時間過長（5.8 秒）
- **Lighthouse LCP**: 5.8s（目標 < 2.5s）
- **原因**:
  - 未使用 SSG/SSR（Client-Side Rendering）
  - 圖片未優化（PNG 格式，無 WebP）
  - 無 CDN（所有資源從 Origin 載入）
  - Bundle 體積 1.5 MB（未 Code Splitting）
- **影響**: 跳出率高，SEO 排名低

### 2. 房源搜尋 API 回應慢（2.3 秒）
- **JMeter P95**: 4,567ms（目標 < 1000ms）
- **原因**:
  - 資料庫 Full Table Scan（無 city 索引）
  - N+1 查詢問題（逐筆查詢圖片）
  - 無快取機制（每次都查資料庫）
- **影響**: 使用者體驗差，伺服器負載高

### 3. 圖片載入慢（每張 > 2 秒）
- **問題**:
  - 圖片未壓縮（5-10 MB/張）
  - 未使用 CDN
  - 無 Lazy Loading
  - 無 WebP 格式
- **影響**: LCP 延遲，浪費頻寬

### 4. 無快取機制
- **問題**:
  - 無 Redis 快取
  - 無 HTTP Cache Headers
  - 無 Service Worker
- **影響**: 重複請求，伺服器壓力大

## ⚠️  中等問題（Medium）

### 5. Bundle 體積過大（1.5 MB）
- **原因**:
  - 未 Code Splitting
  - 包含未使用的 Library
  - 無 Tree Shaking
- **影響**: FCP/LCP 延遲

### 6. 資料庫缺少索引
- **缺少索引**:
  - properties.city
  - properties.created_at
  - bookings (user_id, status)
  - users.email
- **影響**: 查詢慢，CPU 使用率高

## 📊 效能指標彙總

| 指標 | 目前 | 目標 | 差距 |
|------|------|------|------|
| Lighthouse Performance | 45 | 90+ | 🔴 -45 |
| LCP | 5.8s | < 2.5s | 🔴 +3.3s |
| FCP | 3.2s | < 1.8s | 🔴 +1.4s |
| TBT | 850ms | < 200ms | 🔴 +650ms |
| CLS | 0.18 | < 0.1 | ⚠️  +0.08 |
| 房源搜尋 API (P95) | 4,567ms | < 1000ms | 🔴 +3,567ms |
| 資料庫查詢 | 1,234ms | < 100ms | 🔴 +1,134ms |
```

---

### 階段 2：前端效能優化（3-4 週）

#### 3.2.1 Next.js 14 App Router 遷移 + SSR/SSG

**在 Claude Code 輸入**:
```
請協助將 Next.js 13 Pages Router 遷移至 Next.js 14 App Router，並實作 SSR/SSG：

優化項目：
1. 首頁使用 SSG（Static Site Generation）
2. 房源列表使用 SSR（Server-Side Rendering）
3. 房源詳情使用 ISR（Incremental Static Regeneration，每 5 分鐘更新）
4. 啟用 React Server Components

目標：
- LCP < 2.5s
- FCP < 1.8s

請提供遷移指南和程式碼範例。
```

**Claude Code 生成**:

**1. 升級 Next.js** (`package.json`):
```json
{
  "dependencies": {
    "next": "14.0.4",  // ✅ 升級到 14.x
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
```

**2. 遷移首頁（SSG）** (`app/page.tsx`):
```typescript
import { Suspense } from 'react';
import FeaturedProperties from '@/components/FeaturedProperties';
import SearchHero from '@/components/SearchHero';
import { getFeaturedProperties } from '@/lib/api';

// ✅ SSG: 編譯時生成靜態頁面
export const revalidate = false;  // 永不重新驗證（純靜態）

export default async function HomePage() {
  // ✅ Server Component: 在伺服器端獲取資料
  const featuredProperties = await getFeaturedProperties();

  return (
    <main>
      {/* Client Component: 搜尋表單 */}
      <SearchHero />

      {/* Server Component: 精選房源 */}
      <Suspense fallback={<FeaturedSkeleton />}>
        <FeaturedProperties properties={featuredProperties} />
      </Suspense>
    </main>
  );
}

function FeaturedSkeleton() {
  return (
    <div className="grid grid-cols-3 gap-6">
      {[1, 2, 3].map((i) => (
        <div key={i} className="animate-pulse bg-gray-200 h-64 rounded-lg" />
      ))}
    </div>
  );
}
```

**3. 房源列表（SSR）** (`app/properties/page.tsx`):
```typescript
import { searchProperties } from '@/lib/api';
import PropertyList from '@/components/PropertyList';

// ✅ SSR: 每次請求都重新獲取資料
export const dynamic = 'force-dynamic';

interface PageProps {
  searchParams: {
    city?: string;
    maxPrice?: string;
    minGuests?: string;
  };
}

export default async function PropertiesPage({ searchParams }: PageProps) {
  // ✅ Server Component: 伺服器端獲取資料
  const properties = await searchProperties({
    city: searchParams.city,
    maxPrice: searchParams.maxPrice ? Number(searchParams.maxPrice) : undefined,
    minGuests: searchParams.minGuests ? Number(searchParams.minGuests) : undefined,
  });

  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-3xl font-bold mb-6">搜尋房源</h1>
      <PropertyList properties={properties} />
    </div>
  );
}
```

**4. 房源詳情（ISR）** (`app/properties/[id]/page.tsx`):
```typescript
import { notFound } from 'next/navigation';
import { getPropertyById } from '@/lib/api';
import PropertyDetail from '@/components/PropertyDetail';
import BookingForm from '@/components/BookingForm';

// ✅ ISR: 每 5 分鐘重新驗證
export const revalidate = 300;

interface PageProps {
  params: { id: string };
}

// ✅ generateStaticParams: 預先生成熱門房源頁面
export async function generateStaticParams() {
  const popularPropertyIds = await getPopularPropertyIds();  // 例如前 100 個熱門房源

  return popularPropertyIds.map((id) => ({
    id: String(id),
  }));
}

// ✅ generateMetadata: 動態 SEO
export async function generateMetadata({ params }: PageProps) {
  const property = await getPropertyById(params.id);

  if (!property) {
    return { title: '房源不存在' };
  }

  return {
    title: `${property.title} | 優質民宿`,
    description: property.description,
    openGraph: {
      title: property.title,
      description: property.description,
      images: [property.imageUrls[0]],
    },
  };
}

export default async function PropertyDetailPage({ params }: PageProps) {
  const property = await getPropertyById(params.id);

  if (!property) {
    notFound();
  }

  return (
    <div className="container mx-auto px-4 py-8">
      <div className="grid grid-cols-3 gap-8">
        <div className="col-span-2">
          {/* Server Component: 房源詳情 */}
          <PropertyDetail property={property} />
        </div>

        <div className="col-span-1">
          {/* Client Component: 訂房表單 */}
          <BookingForm property={property} />
        </div>
      </div>
    </div>
  );
}
```

#### 3.2.2 圖片優化（Next.js Image + CDN）

**在 Claude Code 輸入**:
```
請優化圖片載入效能：

優化項目：
1. 使用 Next.js Image 組件（自動優化、Lazy Loading）
2. 圖片壓縮（轉換為 WebP）
3. 配置 CDN（Google Cloud CDN）
4. 響應式圖片（srcset）

請提供完整方案。
```

**Claude Code 生成**:

**1. Next.js Image 配置** (`next.config.js`):
```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // ✅ 配置外部圖片域名
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'storage.googleapis.com',
        pathname: '/bnb-platform-images/**',
      },
    ],
    // ✅ 圖片格式優化
    formats: ['image/webp', 'image/avif'],
    // ✅ 圖片尺寸配置
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
    // ✅ 最小化快取時間
    minimumCacheTTL: 60 * 60 * 24 * 365,  // 1 年
  },
};

module.exports = nextConfig;
```

**2. 使用 Next.js Image** (`components/PropertyCard.tsx`):
```typescript
import Image from 'next/image';

interface PropertyCardProps {
  property: Property;
}

export default function PropertyCard({ property }: PropertyCardProps) {
  return (
    <div className="bg-white rounded-lg shadow-md overflow-hidden">
      {/* ✅ Next.js Image: 自動優化、Lazy Loading、WebP */}
      <div className="relative h-48">
        <Image
          src={property.imageUrls[0]}
          alt={property.title}
          fill
          sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw"
          className="object-cover"
          priority={false}  // ✅ Lazy Loading
        />
      </div>

      <div className="p-4">
        <h3 className="font-semibold text-lg">{property.title}</h3>
        <p className="text-gray-600">{property.city}</p>
        <span className="text-xl font-bold">NT$ {property.pricePerNight}</span>
      </div>
    </div>
  );
}
```

**3. 圖片壓縮腳本** (`scripts/optimize-images.sh`):
```bash
#!/bin/bash

# 批次壓縮圖片並上傳至 Google Cloud Storage

INPUT_DIR="public/images"
OUTPUT_DIR="public/images/optimized"
GCS_BUCKET="gs://bnb-platform-images"

mkdir -p $OUTPUT_DIR

echo "🖼️  開始壓縮圖片..."

# 轉換為 WebP（80% 品質）
find $INPUT_DIR -type f \( -name "*.jpg" -o -name "*.png" \) | while read img; do
    filename=$(basename "$img")
    output_file="$OUTPUT_DIR/${filename%.*}.webp"

    echo "壓縮: $filename"
    cwebp -q 80 "$img" -o "$output_file"
done

echo "✅ 壓縮完成！"

echo "📤 上傳至 Google Cloud Storage..."
gsutil -m rsync -r $OUTPUT_DIR $GCS_BUCKET

echo "✅ 上傳完成！"
```

**4. Google Cloud CDN 配置** (`infrastructure/terraform/cdn.tf`):
```hcl
# Cloud Storage Bucket（圖片儲存）
resource "google_storage_bucket" "images" {
  name          = "bnb-platform-images"
  location      = "ASIA-EAST1"
  force_destroy = false

  uniform_bucket_level_access = true

  cors {
    origin          = ["https://bnb-platform.com"]
    method          = ["GET", "HEAD"]
    response_header = ["*"]
    max_age_seconds = 3600
  }
}

# Backend Bucket（CDN 後端）
resource "google_compute_backend_bucket" "image_backend" {
  name        = "image-backend"
  bucket_name = google_storage_bucket.images.name
  enable_cdn  = true

  cdn_policy {
    cache_mode        = "CACHE_ALL_STATIC"
    default_ttl       = 3600
    max_ttl           = 86400
    client_ttl        = 3600
    negative_caching  = true
  }
}

# URL Map（路由）
resource "google_compute_url_map" "cdn" {
  name            = "cdn-url-map"
  default_service = google_compute_backend_bucket.image_backend.id

  host_rule {
    hosts        = ["cdn.bnb-platform.com"]
    path_matcher = "images"
  }

  path_matcher {
    name            = "images"
    default_service = google_compute_backend_bucket.image_backend.id
  }
}
```

**效能對比**:
```
優化前：
- 圖片格式: PNG/JPG
- 平均大小: 5-10 MB/張
- 載入時間: > 2 秒/張
- CDN: 無

優化後：
- 圖片格式: WebP
- 平均大小: 200-500 KB/張  ✅ 減少 95%
- 載入時間: < 300ms/張      ✅ 提升 85%
- CDN: Google Cloud CDN     ✅ 全球加速
```

#### 3.2.3 Bundle 優化（Code Splitting + Tree Shaking）

**在 Claude Code 輸入**:
```
請優化 Bundle 體積：

目標：從 1.5 MB 減至 < 500 KB

優化項目：
1. Code Splitting（路由層級）
2. Dynamic Import（按需載入）
3. Tree Shaking（移除未使用程式碼）
4. 分析 Bundle（webpack-bundle-analyzer）

請提供完整方案。
```

**Claude Code 生成**:

**1. 安裝分析工具**:
```bash
npm install --save-dev @next/bundle-analyzer
```

**2. 配置 Bundle Analyzer** (`next.config.js`):
```javascript
const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
});

/** @type {import('next').NextConfig} */
const nextConfig = {
  // ✅ 啟用 SWC Minification（更快）
  swcMinify: true,

  // ✅ 移除 console.log（Production）
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production',
  },

  // ✅ 實驗性功能
  experimental: {
    optimizePackageImports: ['@mui/material', 'lodash', 'date-fns'],
  },
};

module.exports = withBundleAnalyzer(nextConfig);
```

**3. Dynamic Import（按需載入）** (`components/BookingModal.tsx`):
```typescript
'use client';

import { useState } from 'react';
import dynamic from 'next/dynamic';

// ✅ Dynamic Import: Modal 只在需要時載入
const BookingModal = dynamic(() => import('@/components/BookingModal'), {
  loading: () => <p>載入中...</p>,
  ssr: false,  // ✅ 不在伺服器端渲染
});

export default function PropertyDetail({ property }: PropertyDetailProps) {
  const [showBookingModal, setShowBookingModal] = useState(false);

  return (
    <div>
      <button onClick={() => setShowBookingModal(true)}>
        立即預訂
      </button>

      {/* ✅ Modal 只在 showBookingModal = true 時載入 */}
      {showBookingModal && (
        <BookingModal
          property={property}
          onClose={() => setShowBookingModal(false)}
        />
      )}
    </div>
  );
}
```

**4. 優化第三方 Library 引入**:
```typescript
// ❌ 錯誤：引入整個 lodash
import _ from 'lodash';
const result = _.groupBy(data, 'category');

// ✅ 正確：只引入需要的函式
import groupBy from 'lodash/groupBy';
const result = groupBy(data, 'category');

// ❌ 錯誤：引入整個 date-fns
import * as dateFns from 'date-fns';
const formatted = dateFns.format(new Date(), 'yyyy-MM-dd');

// ✅ 正確：只引入需要的函式
import { format } from 'date-fns';
const formatted = format(new Date(), 'yyyy-MM-dd');
```

**5. 分析 Bundle**:
```bash
# 分析 Bundle 組成
ANALYZE=true npm run build

# 瀏覽器會自動開啟分析報告
```

**6. 移除未使用的 Library** (`package.json`):
```bash
# 找出未使用的依賴
npm install -g depcheck
depcheck

# 移除未使用的 Package
npm uninstall moment  # 改用 date-fns（更輕量）
npm uninstall jquery  # 不需要 jQuery
npm uninstall bootstrap  # 改用 Tailwind CSS
```

**Bundle 對比**:
```
優化前：
- 總體積: 1.5 MB
- JS Chunk: 1.2 MB
- CSS: 300 KB

優化後：
- 總體積: 420 KB  ✅ 減少 72%
- 初始載入: 180 KB
- 動態載入: 240 KB
```

---

### 階段 3：後端效能優化（3 週）

#### 3.3.1 修復資料庫 N+1 查詢問題

**在 Claude Code 輸入**:
```
請修復後端 N+1 查詢問題：

問題 API: GET /api/properties
目前實作：逐筆查詢圖片（N+1 問題）

目標：
- 使用 JOIN 一次查詢
- 回應時間 < 500ms

請提供優化後的完整程式碼。
```

**優化前** (`PropertyController.java`):
```java
@RestController
@RequestMapping("/api/properties")
public class PropertyController {

    @Autowired
    private PropertyRepository propertyRepository;

    @Autowired
    private PropertyImageRepository imageRepository;

    // ❌ N+1 問題
    @GetMapping
    public ResponseEntity<List<PropertyDTO>> searchProperties(
        @RequestParam(required = false) String city,
        @RequestParam(required = false) BigDecimal maxPrice
    ) {
        // 1 次查詢：取得所有房源
        List<Property> properties = propertyRepository.findByCityAndMaxPrice(city, maxPrice);

        // N 次查詢：逐筆查詢圖片
        List<PropertyDTO> dtos = properties.stream()
            .map(property -> {
                // ❌ 每個房源都查一次圖片
                List<PropertyImage> images = imageRepository.findByPropertyId(property.getId());
                return PropertyMapper.toDTO(property, images);
            })
            .collect(Collectors.toList());

        return ResponseEntity.ok(dtos);
    }
}
```

**優化後** (`PropertyController.java` + `PropertyRepository.java`):

**1. Entity 關聯** (`Property.java`):
```java
@Entity
@Table(name = "properties")
public class Property {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String title;
    private String description;
    private String city;
    private BigDecimal pricePerNight;

    // ✅ One-to-Many 關聯（Lazy Loading）
    @OneToMany(mappedBy = "property", fetch = FetchType.LAZY)
    private List<PropertyImage> images = new ArrayList<>();

    // ✅ One-to-Many 關聯
    @OneToMany(mappedBy = "property", fetch = FetchType.LAZY)
    private List<Review> reviews = new ArrayList<>();

    // Getters & Setters...
}
```

**2. Repository（使用 @EntityGraph）** (`PropertyRepository.java`):
```java
@Repository
public interface PropertyRepository extends JpaRepository<Property, Long> {

    // ✅ 使用 @EntityGraph 避免 N+1 問題
    @EntityGraph(attributePaths = {"images", "reviews"})
    @Query("SELECT DISTINCT p FROM Property p " +
           "WHERE (:city IS NULL OR p.city = :city) " +
           "AND (:maxPrice IS NULL OR p.pricePerNight <= :maxPrice) " +
           "AND p.status = 'ACTIVE'")
    List<Property> findByCityAndMaxPrice(
        @Param("city") String city,
        @Param("maxPrice") BigDecimal maxPrice
    );

    // ✅ 或使用 JOIN FETCH
    @Query("SELECT p FROM Property p " +
           "LEFT JOIN FETCH p.images " +
           "LEFT JOIN FETCH p.reviews " +
           "WHERE (:city IS NULL OR p.city = :city) " +
           "AND (:maxPrice IS NULL OR p.pricePerNight <= :maxPrice)")
    List<Property> findByCityAndMaxPriceWithImages(
        @Param("city") String city,
        @Param("maxPrice") BigDecimal maxPrice
    );
}
```

**3. Controller 優化**:
```java
@RestController
@RequestMapping("/api/properties")
public class PropertyController {

    @Autowired
    private PropertyRepository propertyRepository;

    // ✅ 單次查詢，無 N+1 問題
    @GetMapping
    public ResponseEntity<List<PropertyDTO>> searchProperties(
        @RequestParam(required = false) String city,
        @RequestParam(required = false) BigDecimal maxPrice
    ) {
        List<Property> properties = propertyRepository.findByCityAndMaxPrice(city, maxPrice);

        // ✅ 圖片已在 JOIN 中載入，無額外查詢
        List<PropertyDTO> dtos = properties.stream()
            .map(PropertyMapper::toDTO)
            .collect(Collectors.toList());

        return ResponseEntity.ok(dtos);
    }
}
```

**效能對比**:
```
優化前（N+1 問題）:
- SQL 查詢次數: 501 次（1 + 500）
- 回應時間: 2,345 ms

優化後（JOIN FETCH）:
- SQL 查詢次數: 1 次
- 回應時間: 234 ms  ✅ 提升 90%
```

#### 3.3.2 新增資料庫索引

**在 Claude Code 輸入**:
```
請根據慢查詢分析結果，新增資料庫索引：

需新增索引：
1. properties.city
2. properties (city, price_per_night)
3. bookings (user_id, status)
4. users.email

請生成 Flyway 遷移腳本。
```

**Claude Code 生成** (`src/main/resources/db/migration/V2__add_performance_indexes.sql`):
```sql
-- V2__add_performance_indexes.sql
-- 新增效能索引

-- 1. properties.city 索引（單一欄位）
CREATE INDEX idx_properties_city ON properties(city);

-- 2. properties (city, price_per_night) 複合索引（常見查詢）
CREATE INDEX idx_properties_city_price ON properties(city, price_per_night DESC);

-- 3. properties.created_at 索引（排序用）
CREATE INDEX idx_properties_created_at ON properties(created_at DESC);

-- 4. bookings (user_id, status) 複合索引
CREATE INDEX idx_bookings_user_status ON bookings(user_id, status);

-- 5. bookings.property_id 索引（JOIN 用）
CREATE INDEX idx_bookings_property_id ON bookings(property_id);

-- 6. users.email 索引（登入查詢）
CREATE INDEX idx_users_email ON users(email);

-- 7. reviews.property_id 索引（房源評價查詢）
CREATE INDEX idx_reviews_property_id ON reviews(property_id);

-- 8. property_images.property_id 索引（已存在於 FK，但確保）
CREATE INDEX IF NOT EXISTS idx_property_images_property_id ON property_images(property_id);

-- 分析表格（PostgreSQL）
ANALYZE properties;
ANALYZE bookings;
ANALYZE users;
ANALYZE reviews;
ANALYZE property_images;
```

**執行遷移**:
```bash
# Flyway 會自動執行遷移
./mvnw flyway:migrate

# 或在應用程式啟動時自動執行（application.yml）
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
```

**索引效能對比**:
```sql
-- 優化前（無索引）
EXPLAIN ANALYZE
SELECT * FROM properties WHERE city = '台北' AND price_per_night <= 5000;

-- 結果:
-- Seq Scan on properties  (cost=0.00..245.00 rows=500 width=150) (actual time=1234.567..1234.890 rows=500 loops=1)
-- Planning Time: 0.123 ms
-- Execution Time: 1234.890 ms  🔴

-- 優化後（有索引）
EXPLAIN ANALYZE
SELECT * FROM properties WHERE city = '台北' AND price_per_night <= 5000;

-- 結果:
-- Index Scan using idx_properties_city_price on properties  (cost=0.28..45.12 rows=500 width=150) (actual time=12.345..15.678 rows=500 loops=1)
-- Planning Time: 0.234 ms
-- Execution Time: 15.890 ms  ✅ 提升 99%
```

#### 3.3.3 整合 Redis 快取

**在 Claude Code 輸入**:
```
請整合 Redis 快取，減少資料庫查詢：

快取策略：
1. 房源搜尋結果（5 分鐘 TTL）
2. 房源詳情（10 分鐘 TTL）
3. 熱門房源清單（1 小時 TTL）

請提供完整實作。
```

**Claude Code 生成**:

**1. 依賴** (`pom.xml`):
```xml
<dependencies>
    <!-- Spring Data Redis -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>

    <!-- Lettuce（Redis Client） -->
    <dependency>
        <groupId>io.lettuce</groupId>
        <artifactId>lettuce-core</artifactId>
    </dependency>

    <!-- Jackson（JSON 序列化） -->
    <dependency>
        <groupId>com.fasterxml.jackson.datatype</groupId>
        <artifactId>jackson-datatype-jsr310</artifactId>
    </dependency>
</dependencies>
```

**2. Redis 配置** (`RedisConfig.java`):
```java
@Configuration
@EnableCaching
public class RedisConfig {

    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory connectionFactory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(connectionFactory);

        // ✅ 使用 Jackson2JsonRedisSerializer（JSON 格式）
        Jackson2JsonRedisSerializer<Object> serializer = new Jackson2JsonRedisSerializer<>(Object.class);
        ObjectMapper mapper = new ObjectMapper();
        mapper.setVisibility(PropertyAccessor.ALL, JsonAutoDetect.Visibility.ANY);
        mapper.activateDefaultTyping(mapper.getPolymorphicTypeValidator(), ObjectMapper.DefaultTyping.NON_FINAL);
        mapper.registerModule(new JavaTimeModule());  // ✅ 支援 LocalDateTime
        serializer.setObjectMapper(mapper);

        template.setKeySerializer(new StringRedisSerializer());
        template.setValueSerializer(serializer);
        template.setHashKeySerializer(new StringRedisSerializer());
        template.setHashValueSerializer(serializer);

        return template;
    }

    @Bean
    public CacheManager cacheManager(RedisConnectionFactory connectionFactory) {
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig()
            .entryTtl(Duration.ofMinutes(5))  // ✅ 預設 TTL: 5 分鐘
            .serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(new StringRedisSerializer()))
            .serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(new Jackson2JsonRedisSerializer<>(Object.class)))
            .disableCachingNullValues();

        // ✅ 不同快取不同 TTL
        Map<String, RedisCacheConfiguration> cacheConfigurations = new HashMap<>();
        cacheConfigurations.put("properties", config.entryTtl(Duration.ofMinutes(5)));
        cacheConfigurations.put("propertyDetail", config.entryTtl(Duration.ofMinutes(10)));
        cacheConfigurations.put("featuredProperties", config.entryTtl(Duration.ofHours(1)));

        return RedisCacheManager.builder(connectionFactory)
            .cacheDefaults(config)
            .withInitialCacheConfigurations(cacheConfigurations)
            .build();
    }
}
```

**3. Service 層使用快取** (`PropertyService.java`):
```java
@Service
public class PropertyService {

    @Autowired
    private PropertyRepository propertyRepository;

    // ✅ 快取房源搜尋結果（5 分鐘）
    @Cacheable(value = "properties", key = "#city + '_' + #maxPrice")
    public List<PropertyDTO> searchProperties(String city, BigDecimal maxPrice) {
        List<Property> properties = propertyRepository.findByCityAndMaxPrice(city, maxPrice);
        return properties.stream()
            .map(PropertyMapper::toDTO)
            .collect(Collectors.toList());
    }

    // ✅ 快取房源詳情（10 分鐘）
    @Cacheable(value = "propertyDetail", key = "#id")
    public PropertyDTO getPropertyById(Long id) {
        Property property = propertyRepository.findById(id)
            .orElseThrow(() -> new PropertyNotFoundException(id));
        return PropertyMapper.toDTO(property);
    }

    // ✅ 快取熱門房源（1 小時）
    @Cacheable(value = "featuredProperties")
    public List<PropertyDTO> getFeaturedProperties() {
        List<Property> properties = propertyRepository.findTop10ByOrderByViewCountDesc();
        return properties.stream()
            .map(PropertyMapper::toDTO)
            .collect(Collectors.toList());
    }

    // ✅ 新增/更新房源時清除快取
    @CacheEvict(value = {"properties", "featuredProperties"}, allEntries = true)
    public PropertyDTO createProperty(CreatePropertyRequest request) {
        Property property = PropertyMapper.toEntity(request);
        Property saved = propertyRepository.save(property);
        return PropertyMapper.toDTO(saved);
    }

    // ✅ 更新房源詳情時清除該房源快取
    @CacheEvict(value = "propertyDetail", key = "#id")
    public PropertyDTO updateProperty(Long id, UpdatePropertyRequest request) {
        Property property = propertyRepository.findById(id)
            .orElseThrow(() -> new PropertyNotFoundException(id));
        PropertyMapper.updateEntity(property, request);
        Property updated = propertyRepository.save(property);
        return PropertyMapper.toDTO(updated);
    }
}
```

**4. application.yml**:
```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD:}
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2
          max-wait: 3000ms
```

**5. Docker Compose** (`docker-compose.yml`):
```yaml
version: '3.8'
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes

volumes:
  redis-data:
```

**快取效能對比**:
```
第一次請求（Cache Miss）:
- 回應時間: 234 ms
- 查詢資料庫: ✅

第二次請求（Cache Hit）:
- 回應時間: 12 ms  ✅ 提升 95%
- 查詢資料庫: ❌（從 Redis 讀取）

快取命中率（1 小時後）: 87%
```

---

### 階段 4：最終驗證與報告（1 週）

#### 3.4.1 重新執行效能測試

**在 Claude Code 輸入**:
```
請重新執行所有效能測試，生成最終報告：

1. Lighthouse 測試（前端）
2. JMeter 壓測（後端 API）
3. 資料庫查詢效能
4. Google PageSpeed Insights

生成最終報告：`Docs/reports/performance-optimization-final-report.md`
```

**執行測試**:
```bash
# 1. Lighthouse 測試
./performance-analysis/lighthouse/run-lighthouse.sh

# 2. JMeter 壓測
jmeter -n -t performance-analysis/profiling/api-load-test.jmx \
  -l performance-analysis/profiling/jmeter-results-final.csv \
  -e -o performance-analysis/profiling/jmeter-report-final

# 3. 資料庫查詢
psql -h localhost -U postgres -d bnb_platform -f database/analyze-slow-queries.sql \
  > performance-analysis/database/slow-queries-final.txt
```

**預期輸出** (`Docs/reports/performance-optimization-final-report.md`):
```markdown
# 效能優化最終報告

## 📊 效能指標對比

### 1. Lighthouse 分數

| 指標 | 優化前 | 優化後 | 改善 |
|------|--------|--------|------|
| Performance | 45 | 94 | ✅ **+109%** |
| LCP | 5.8s | 1.2s | ✅ **79%** |
| FCP | 3.2s | 0.8s | ✅ **75%** |
| TBT | 850ms | 120ms | ✅ **86%** |
| CLS | 0.18 | 0.05 | ✅ **72%** |
| Speed Index | 4.5s | 1.8s | ✅ **60%** |

### 2. 後端 API 效能

| API | 優化前 (P95) | 優化後 (P95) | 改善 |
|-----|-------------|-------------|------|
| GET /api/properties | 4,567ms | 345ms | ✅ **92%** |
| GET /api/properties/{id} | 2,345ms | 123ms | ✅ **95%** |
| POST /api/bookings | 3,456ms | 456ms | ✅ **87%** |
| 吞吐量 | 42.5 req/s | 485 req/s | ✅ **+1041%** |

### 3. 資料庫效能

| 指標 | 優化前 | 優化後 | 改善 |
|------|--------|--------|------|
| 平均查詢時間 | 1,234ms | 15ms | ✅ **99%** |
| 慢查詢數量 | 23 | 0 | ✅ **100%** |
| N+1 查詢 | 10,567 次 | 0 | ✅ **100%** |
| 快取命中率 | 0% | 87% | ✅ **+87%** |

### 4. 圖片載入

| 指標 | 優化前 | 優化後 | 改善 |
|------|--------|--------|------|
| 平均大小 | 5-10 MB | 200-500 KB | ✅ **95%** |
| 載入時間 | > 2s | < 300ms | ✅ **85%** |
| CDN 使用 | ❌ | ✅ | - |

### 5. Bundle 體積

| 指標 | 優化前 | 優化後 | 改善 |
|------|--------|--------|------|
| 總體積 | 1.5 MB | 420 KB | ✅ **72%** |
| 初始載入 | 1.5 MB | 180 KB | ✅ **88%** |
| 動態載入 | 0 | 240 KB | - |

## ✅ 已完成優化項目

### 前端優化
- [x] Next.js 14 App Router 遷移
- [x] SSG/SSR/ISR 實作
- [x] React Server Components
- [x] Next.js Image 優化
- [x] 圖片轉換為 WebP
- [x] Google Cloud CDN
- [x] Code Splitting
- [x] Dynamic Import
- [x] Bundle 體積優化

### 後端優化
- [x] 修復 N+1 查詢問題
- [x] 新增資料庫索引（8 個）
- [x] 整合 Redis 快取
- [x] API 回應時間優化
- [x] 吞吐量提升

### 資料庫優化
- [x] PostgreSQL 慢查詢分析
- [x] 索引優化
- [x] 查詢效能提升 99%
- [x] 消除所有 N+1 查詢

## 🎯 使用者體驗改善

| 場景 | 優化前 | 優化後 |
|------|--------|--------|
| 開啟首頁 | 5.8 秒 | 1.2 秒 ✅ |
| 搜尋房源 | 4.5 秒 | 0.35 秒 ✅ |
| 查看房源詳情 | 2.3 秒 | 0.12 秒 ✅ |
| 圖片載入 | 2+ 秒/張 | < 0.3 秒/張 ✅ |

## 📈 商業影響

### SEO 排名
- Google PageSpeed Insights: 45 → 94
- Core Web Vitals: ❌ → ✅
- 預估自然流量提升: **+150%**

### 轉換率
- 頁面載入速度提升 **79%**
- 預估跳出率降低: **-40%**
- 預估轉換率提升: **+25%**

### 伺服器成本
- 資料庫查詢減少: **99%**
- 頻寬成本降低: **60%**（CDN + 圖片優化）
- 伺服器負載降低: **85%**
```

---

## 附錄：命令速查表

### AISDLC Workflow

| 階段 | Workflow | Claude Code 命令 |
|------|----------|-----------------|
| 效能分析 | performance-profiling-and-analysis | `請載入 AISDLC_INIT.md，執行「performance-profiling-and-analysis」workflow` |

### Lighthouse 測試

```bash
# 單頁測試
lighthouse http://localhost:3000 \
  --output=html \
  --output-path=report.html \
  --chrome-flags="--headless"

# 批次測試
./performance-analysis/lighthouse/run-lighthouse.sh
```

### JMeter 壓測

```bash
# 執行測試
jmeter -n -t api-load-test.jmx \
  -l results.csv \
  -e -o report-folder

# 查看報告
open report-folder/index.html
```

### PostgreSQL 慢查詢

```bash
# 連接資料庫
psql -h localhost -U postgres -d bnb_platform

# 執行分析
\i database/analyze-slow-queries.sql

# 輸出到檔案
psql -h localhost -U postgres -d bnb_platform \
  -f database/analyze-slow-queries.sql \
  > slow-queries-report.txt
```

### Next.js Bundle 分析

```bash
# 分析 Bundle
ANALYZE=true npm run build

# 查看報告（自動開啟瀏覽器）
```

### Google Cloud CDN

```bash
# 上傳圖片到 Cloud Storage
gsutil -m rsync -r public/images/optimized gs://bnb-platform-images

# 設定快取控制
gsutil -m setmeta -h "Cache-Control:public, max-age=31536000" \
  gs://bnb-platform-images/**
```

### Redis 快取

```bash
# 連接 Redis
redis-cli

# 查看所有 Key
KEYS *

# 查看快取命中率
INFO stats

# 清除所有快取
FLUSHALL
```

---

## 🎯 效能優化時程表（總計 9-11 週）

| 階段 | 週數 | 主要工作 |
|------|-----|---------|
| 1. 效能基準測試與診斷 | 2 週 | Lighthouse、JMeter、資料庫分析、診斷報告 |
| 2. 前端效能優化 | 3-4 週 | Next.js 14 App Router、SSR/SSG/ISR、圖片優化、CDN、Bundle 優化 |
| 3. 後端效能優化 | 3 週 | 修復 N+1、新增索引、Redis 快取 |
| 4. 最終驗證與報告 | 1 週 | 重新測試、生成報告、Beta 測試 |

**總計**: 9-11 週（約 2.5 個月）

---

## 📚 相關文檔

- [AISDLC_INIT.md](../../AISDLC_INIT.md)
- [performance-profiling-and-analysis.md](../../workflow/scenario-specific/performance-profiling-and-analysis.md)
- [Next.js Image 優化](https://nextjs.org/docs/app/building-your-application/optimizing/images)
- [Google Cloud CDN 文檔](https://cloud.google.com/cdn/docs)
- [Spring Boot Redis](https://spring.io/guides/gs/messaging-redis)

---

**版本**: 1.0
**作者**: AISDLC Framework Team
**最後更新**: 2025-12-15
