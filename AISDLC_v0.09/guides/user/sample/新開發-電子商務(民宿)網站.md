# 民宿預訂網站開發完整範例
# AISDLC Framework - E-Commerce Web Development Guide

**版本**: v0.09
**建立日期**: 2025-12-13
**適用情境**: Greenfield Web App 開發（電子商務）
**範例專案**: BnB Booking Platform - 民宿預訂平台

---

## 🎯 專案概述

**專案名稱**: BnB Booking Platform
**專案類型**: 電子商務網站（民宿預訂）
**技術棧**:
- **前端**: React + Next.js 14 (App Router)
- **後端**: Spring Boot 3.x (Java 17)
- **資料庫**: PostgreSQL 15
- **部署**: Google Cloud (Cloud Run + Cloud SQL + Cloud Storage)

**開發工具**: Cursor AI + Claude Code
**AISDLC 版本**: v0.09

---

## 📂 步驟 1: Cursor AI 專案路徑設定

### 1.1 建立 Monorepo 專案結構

```bash
# 建立主專案目錄
mkdir BnB_Booking_Platform
cd BnB_Booking_Platform

# 建立前後端分離結構
mkdir -p {frontend,backend,docs,infra}

# 建立文檔目錄
mkdir -p docs/{requirements,architecture,api,tests}
```

### 1.2 初始化 Git（選用）

```bash
git init
echo "node_modules/" > .gitignore
echo "target/" >> .gitignore
echo ".env" >> .gitignore
```

### 1.3 開啟 Cursor AI

1. 啟動 Cursor AI
2. **File → Open Folder**
3. 選擇 `BnB_Booking_Platform` 資料夾
4. 確認左側檔案樹顯示完整結構

**預期結構**:
```
BnB_Booking_Platform/
├── frontend/          # Next.js 前端
├── backend/           # Spring Boot 後端
├── docs/              # 文檔（PRD/FRD/SRD/API）
└── infra/             # 基礎設施（Docker/K8s）
```

---

## 🔧 步驟 2: 安裝設定 AISDLC Framework

### 2.1 複製 AISDLC 到專案

```bash
# 在專案根目錄執行
cp -r /Users/wuweihong/Cursor_Project/AISDLC_ALL/AISDLC_v0.09 ./AISDLC

# 驗證安裝
ls -la AISDLC/
# 預期輸出: AISDLC_INIT.md, agent/, workflow/, docs_template/, scenarios/, guides/
```

### 2.2 完整專案結構

```
BnB_Booking_Platform/
├── AISDLC/                    # AISDLC Framework
│   ├── AISDLC_INIT.md         # 框架初始化（必讀）
│   ├── scenarios/greenfield/  # Greenfield SOP
│   ├── agent/                 # Agent 配置
│   ├── workflow/              # Workflow 定義
│   ├── docs_template/         # 文檔模板
│   └── guides/                # 參考指南
├── frontend/                  # Next.js 14
│   ├── app/
│   ├── components/
│   └── package.json
├── backend/                   # Spring Boot
│   ├── src/
│   ├── pom.xml
│   └── application.yml
├── docs/                      # 專案文檔
│   ├── requirements/          # PRD, FRD
│   ├── architecture/          # SRD, C4 Model
│   ├── api/                   # API 規格
│   └── tests/                 # 測試計畫
└── infra/                     # DevOps
    ├── docker-compose.yml
    └── kubernetes/
```

---

## 💬 步驟 3: Claude Code 開發流程（完整範例）

### 階段 0: 框架初始化

**Cursor AI → Chat 面板 → 輸入指令**:

```
請載入 AISDLC Framework 初始化文檔：

讀取並執行：AISDLC/AISDLC_INIT.md

專案類型：Greenfield Web App (E-Commerce)
```

**預期回應**:
```
✅ AISDLC Framework v0.09 已載入
✅ Workflow-Agent 映射表已加載
✅ 準備開始 Greenfield 開發流程
```

---

### 階段 1: 需求蒐集（2-3 小時）

#### 1.1 啟動需求分析

**輸入指令**:
```
我要開發民宿預訂網站，請開始執行 Greenfield SOP。

專案名稱：BnB Booking Platform
平台：Web App (前後端分離)
技術棧：
- 前端：React + Next.js 14
- 後端：Spring Boot 3
- 資料庫：PostgreSQL
目標：提供民宿預訂、房源管理、訂單管理功能
```

**AI 回應**:
```
✅ SA-Analyst (Amanda) 已載入 - 準備進行需求分析
✅ BA-Business-Analyst (Beatrice) 已載入 - 準備驗證商業需求

【需求蒐集問卷】
請回答以下問題以完成需求分析...
```

#### 1.2 回答需求問卷

**AI 提問 + 你的回答範例**:

```
Q1. 核心功能需求？
A1.
   【房客端】
   - 搜尋房源（地點、日期、人數篩選）
   - 查看房源詳情（照片、設施、評價）
   - 預訂房間（選擇日期、填寫資料）
   - 線上付款（信用卡、第三方支付）
   - 訂單管理（查看、取消、評價）

   【房東端】
   - 房源管理（新增、編輯、下架）
   - 日曆管理（設定可訂日期、價格）
   - 訂單管理（接受、拒絕、確認入住）
   - 收益統計（訂單報表、收入分析）

Q2. 目標使用者？
A2.
   - 房客：25-45 歲旅遊愛好者
   - 房東：35-60 歲民宿經營者
   - 預估使用者：10,000 房客 + 500 房東（首年）

Q3. 效能需求？
A3.
   - 搜尋回應時間 < 500ms
   - 支援 1000 concurrent users
   - 99.9% uptime (SLA)

Q4. 付款整合？
A4.
   - Phase 1: Stripe (信用卡)
   - Phase 2: PayPal, Apple Pay

Q5. 預算與時程？
A5.
   - 預算：$150k (開發 + 雲端首年)
   - 時程：6 個月 MVP 上線
```

#### 1.3 確認 PRD 文檔

**AI 輸出**:
```
✅ 需求分析完成！

【產出文檔】
- docs/requirements/PRD_BnB_Platform_v1.0.md
- docs/requirements/Requirement_Analysis_Report.md
- docs/requirements/User_Journey_Map.md

【識別的 Epic】
- EPIC-001: 房源搜尋與展示
- EPIC-002: 預訂與付款流程
- EPIC-003: 房源管理（房東端）
- EPIC-004: 訂單管理
- EPIC-005: 使用者系統（註冊/登入）

【下一步】
請確認 PRD 文檔，確認後進入 Stage 2（需求驗證與威脅建模）
```

---

### 階段 2: 需求驗證與威脅建模（3-4 小時）

#### 2.1 STRIDE 威脅建模

**輸入指令**:
```
PRD 確認無誤，請進入 Stage 2 需求驗證與威脅建模。
```

**AI 執行**:
```
✅ 正在執行 STRIDE 威脅分析...

【威脅建模結果】
識別 18 個潛在威脅：
- Spoofing (S): 3 個（假冒房東、假冒房客、Session 劫持）
- Tampering (T): 4 個（價格竄改、訂單狀態竄改、SQL Injection、XSS）
- Repudiation (R): 2 個（否認訂單、否認付款）
- Information Disclosure (I): 3 個（個資洩露、付款資訊洩露、API Key 洩露）
- Denial of Service (D): 3 個（DDoS、API 濫用、資料庫過載）
- Elevation of Privilege (E): 3 個（權限提升、越權存取、Admin 帳號破解）

【產出安全需求】
- NFR-SEC-001: JWT 認證授權（對應 S, E）
- NFR-SEC-002: 輸入驗證與防篡改（對應 T）
- NFR-SEC-003: 敏感資料加密（對應 I）
- NFR-SEC-004: 操作日誌與審計（對應 R）
- NFR-SEC-005: API Rate Limiting（對應 D）
- NFR-SEC-006: PCI-DSS 付款安全（對應 I）

【產出文檔】
- docs/requirements/FRD_BnB_Platform_v1.0.md
- docs/requirements/STRIDE_Threat_Analysis.md
- docs/requirements/Security_Requirements_Spec.md
```

#### 2.2 確認 User Story

**AI 輸出 User Story 列表（範例）**:
```
【EPIC-001: 房源搜尋與展示】
- US-001: 使用者可以依地點搜尋房源
  - AC-001-1: 輸入城市名稱顯示該城市所有房源
  - AC-001-2: 支援模糊搜尋（台北 → 台北市、新北市）
  - AT-001-1-1: 搜尋「台北」顯示台北市 20 筆房源

- US-002: 使用者可以依日期篩選可訂房源
  - AC-002-1: 選擇入住/退房日期，僅顯示該期間可訂房源
  - AC-002-2: 已被預訂的房源不顯示
  - AT-002-1-1: 選擇 2025-12-20 ~ 12-22，顯示 15 筆可訂房源

【EPIC-002: 預訂與付款流程】
- US-003: 使用者可以預訂房間
  - AC-003-1: 填寫房客資料（姓名、電話、Email）
  - AC-003-2: 確認訂單總價（房費 + 清潔費 + 稅金）
  - AT-003-1-1: 預訂成功後顯示訂單編號

- US-004: 使用者可以線上付款
  - AC-004-1: 使用 Stripe 信用卡付款
  - AC-004-2: 付款成功後訂單狀態改為「已付款」
  - AT-004-1-1: 模擬付款成功，驗證訂單狀態更新

... (共 45 個 User Story)
```

---

### 階段 3: 技術選型（2-4 小時）

#### 3.1 技術棧評估

**輸入指令**:
```
FRD 確認完成，請進入 Stage 3 技術選型階段。

技術棧偏好：
- 前端：Next.js 14 (App Router + Server Components)
- 後端：Spring Boot 3.2 (Java 17)
- 資料庫：Cloud SQL PostgreSQL 15
- 快取：Memorystore for Redis
- 檔案儲存：Google Cloud Storage
- 付款：Stripe

請進行技術選型評估矩陣分析。
```

**AI 執行**:
```
✅ SD-Architect (Marcus) 已載入 - 準備進行技術選型
✅ 正在使用 Tech_Stack_Selection_Matrix.md 進行評估...

【技術選型評估矩陣】

#### 1. Backend Framework 選型

| 評估維度 | 權重 | Spring Boot | NestJS | Django |
|---------|------|-------------|--------|--------|
| 功能性 | 30% | 5 | 4 | 4 |
| 成本 | 25% | 5 (開源) | 5 | 5 |
| 學習曲線 | 20% | 3 | 4 | 4 |
| 社群支援 | 15% | 5 | 4 | 4 |
| 風險評估 | 10% | 5 | 4 | 4 |
| **加權總分** | 100% | **4.65** | **4.25** | **4.15** |

**決策**: 選擇 **Spring Boot 3.2**
**理由**:
1. 功能性：企業級框架，完整生態系統
2. 效能：Java 虛擬執行緒（Virtual Threads）支援高並發
3. 安全：Spring Security 成熟完善
4. 長期維護：大型專案首選，招募容易

---

#### 2. Frontend Framework 選型

| 評估維度 | 權重 | Next.js 14 | Nuxt 3 | Remix |
|---------|------|-----------|--------|-------|
| 功能性 | 30% | 5 | 4 | 4 |
| 成本 | 25% | 5 | 5 | 5 |
| 學習曲線 | 20% | 4 | 4 | 3 |
| 社群支援 | 15% | 5 | 3 | 3 |
| 風險評估 | 10% | 5 | 4 | 3 |
| **加權總分** | 100% | **4.75** | **4.05** | **3.75** |

**決策**: 選擇 **Next.js 14 (App Router)**
**理由**:
1. SEO 優化：房源頁面需要 SSR/SSG
2. 效能：Server Components 減少 Client Bundle
3. 生態系：最大 React 社群支援
4. 部署：Vercel 無縫整合

---

#### 3. Database 選型

| 評估維度 | 權重 | PostgreSQL | MySQL | MongoDB |
|---------|------|-----------|-------|---------|
| 功能性 | 30% | 5 | 4 | 3 |
| 成本 | 25% | 5 | 5 | 4 |
| 學習曲線 | 20% | 4 | 5 | 4 |
| 社群支援 | 15% | 5 | 5 | 4 |
| 風險評估 | 10% | 5 | 5 | 3 |
| **加權總分** | 100% | **4.75** | **4.70** | **3.60** |

**決策**: 選擇 **PostgreSQL 15**
**理由**:
1. 功能性：JSONB 欄位支援彈性資料、Full-Text Search
2. 可靠性：完整 ACID、資料一致性強
3. 地理位置：PostGIS 支援（房源地圖功能）
4. 效能：Indexing 效能優於 MySQL

---

【最終技術棧】
- **Frontend**: Next.js 14 + TypeScript + Tailwind CSS + Zustand
- **Backend**: Spring Boot 3.2 + Spring Security + Spring Data JPA
- **Database**: PostgreSQL 15 + Redis (Cache)
- **Storage**: Google Cloud Storage (房源照片)
- **Payment**: Stripe API
- **Deployment**: Cloud Run (Backend) + Vercel (Frontend)
- **Monitoring**: Cloud Logging + Cloud Monitoring + Sentry

【產出文檔】
- docs/architecture/Tech_Stack_Selection_Report.md
- docs/architecture/Architecture_Decision_Record.md
- docs/architecture/Cost_Estimation_Report.md (3 年 TCO: $245k)
```

---

### 階段 4-5: 系統架構設計（4-6 小時）

#### 4.1 C4 Model 架構設計

**輸入指令**:
```
技術選型確認，請進入系統架構設計階段（Stage 4-5）。

請設計：
1. C4 Model Level 1-3
2. 資料庫 Schema
3. API 規格總覽
4. 安全架構（對應 NFR-SEC）
5. 高可用性架構（對應 NFR-AVL）
```

**AI 輸出 - C4 Level 2 Container Diagram**:
```
┌──────────────────────────────────────────────────────────────┐
│          BnB Platform - Container Diagram (C4 Level 2)        │
└──────────────────────────────────────────────────────────────┘

👤 房客 (Web Browser)
   │ HTTPS
   ↓
┌────────────────────────┐
│  Next.js Frontend      │  ← Vercel 部署
│  (React 18 + SSR)      │  ← /search, /listing/:id, /booking
└────────────────────────┘
   │ REST API / GraphQL
   ↓
┌────────────────────────┐
│  Cloud Load Balancing  │  ← HTTPS Load Balancer
│  (Global HTTP(S) LB)   │  ← JWT 驗證、Rate Limiting
└────────────────────────┘
   │
   ├──→ ┌───────────────────────┐
   │    │  Auth Service         │  ← Cloud Run
   │    │  (JWT, OAuth 2.0)     │  ← /api/auth/login, /register
   │    └───────────────────────┘
   │
   ├──→ ┌───────────────────────┐
   │    │  Listing Service      │  ← Cloud Run
   │    │  (房源管理)            │  ← /api/listings, /api/search
   │    └───────────────────────┘
   │
   ├──→ ┌───────────────────────┐
   │    │  Booking Service      │  ← Cloud Run
   │    │  (預訂管理)            │  ← /api/bookings, /api/orders
   │    └───────────────────────┘
   │
   └──→ ┌───────────────────────┐
        │  Payment Service      │  ← Cloud Run
        │  (付款整合)            │  ← /api/payments, /stripe/webhook
        └───────────────────────┘
           │
           ↓
        🗄️ PostgreSQL (Cloud SQL)
           ├─ listings (房源)
           ├─ bookings (訂單)
           ├─ users (使用者)
           └─ payments (付款記錄)

        📦 Redis (Memorystore)
           ├─ Session Cache
           ├─ Search Result Cache
           └─ Rate Limiting Counter

        📁 Cloud Storage (GCS)
           └─ Listing Images (房源照片)
```

#### 4.2 資料庫 Schema 設計

**AI 輸出**:
```sql
-- 使用者表
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    role VARCHAR(20) NOT NULL,  -- 'GUEST', 'HOST', 'ADMIN'
    first_name VARCHAR(100),
    last_name VARCHAR(100),
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 房源表
CREATE TABLE listings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    host_id UUID NOT NULL REFERENCES users(id),
    title VARCHAR(255) NOT NULL,
    description TEXT,
    property_type VARCHAR(50),  -- 'HOUSE', 'APARTMENT', 'ROOM'
    max_guests INTEGER NOT NULL,
    bedrooms INTEGER,
    bathrooms INTEGER,
    price_per_night DECIMAL(10,2) NOT NULL,
    cleaning_fee DECIMAL(10,2),
    address JSONB NOT NULL,      -- {city, district, street, lat, lng}
    amenities JSONB,             -- ['WiFi', 'Kitchen', 'Parking']
    status VARCHAR(20) DEFAULT 'ACTIVE',  -- 'ACTIVE', 'INACTIVE', 'DELETED'
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 房源照片表
CREATE TABLE listing_images (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    listing_id UUID NOT NULL REFERENCES listings(id) ON DELETE CASCADE,
    gcs_url VARCHAR(500) NOT NULL,  -- gs://bucket-name/path/to/image.jpg
    display_order INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 訂單表
CREATE TABLE bookings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    listing_id UUID NOT NULL REFERENCES listings(id),
    guest_id UUID NOT NULL REFERENCES users(id),
    check_in_date DATE NOT NULL,
    check_out_date DATE NOT NULL,
    num_guests INTEGER NOT NULL,
    total_price DECIMAL(10,2) NOT NULL,
    status VARCHAR(20) DEFAULT 'PENDING',  -- 'PENDING', 'CONFIRMED', 'CANCELLED', 'COMPLETED'
    guest_info JSONB,                      -- {firstName, lastName, phone, email}
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT check_dates CHECK (check_out_date > check_in_date)
);

-- 付款表
CREATE TABLE payments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    booking_id UUID NOT NULL REFERENCES bookings(id),
    stripe_payment_intent_id VARCHAR(255),
    amount DECIMAL(10,2) NOT NULL,
    currency VARCHAR(3) DEFAULT 'USD',
    status VARCHAR(20) DEFAULT 'PENDING',  -- 'PENDING', 'SUCCESS', 'FAILED', 'REFUNDED'
    payment_method VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 評價表
CREATE TABLE reviews (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    booking_id UUID NOT NULL REFERENCES bookings(id),
    reviewer_id UUID NOT NULL REFERENCES users(id),
    rating INTEGER NOT NULL CHECK (rating BETWEEN 1 AND 5),
    comment TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 索引優化
CREATE INDEX idx_listings_host ON listings(host_id);
CREATE INDEX idx_listings_price ON listings(price_per_night);
CREATE INDEX idx_listings_location ON listings USING GIN (address);
CREATE INDEX idx_bookings_guest ON bookings(guest_id);
CREATE INDEX idx_bookings_dates ON bookings(check_in_date, check_out_date);
```

#### 4.3 安全架構設計

**AI 輸出（對應 NFR-SEC）**:
```
【安全元件設計】

1️⃣ 認證授權（NFR-SEC-001）
- JWT Token (RS256 簽名)
- Access Token: 30 分鐘
- Refresh Token: 30 天（HttpOnly Cookie）
- Spring Security + JWT Filter

2️⃣ 輸入驗證（NFR-SEC-002）
- Spring Validation (@Valid, @NotNull)
- SQL Injection 防護（JPA Prepared Statements）
- XSS 防護（React 自動編碼 + CSP Header）

3️⃣ 敏感資料保護（NFR-SEC-003）
- 密碼：bcrypt (cost 12)
- 付款資訊：僅儲存 Stripe Token，不存信用卡
- HTTPS 強制（HSTS Header）
- PostgreSQL TDE (Transparent Data Encryption)

4️⃣ 操作日誌（NFR-SEC-004）
- AOP 記錄所有 API 呼叫
- Cloud Logging 集中化（Structured Logging）
- 日誌格式：JSON (timestamp, userId, action, result, ip, traceId)
- 整合 Cloud Trace 進行分散式追蹤

5️⃣ Rate Limiting（NFR-SEC-005）
- Cloud Armor (DDoS 防護)
- 登入 API: 5 req/min per IP
- 一般 API: 100 req/min per User
- 整合 Cloud Load Balancing 的 Rate Limiting 功能

6️⃣ PCI-DSS 付款安全（NFR-SEC-006）
- 使用 Stripe Hosted Checkout（不經手信用卡）
- Webhook 簽名驗證
- 付款金額後端重算（防竄改）
```

#### 4.4 高可用性架構

**AI 輸出（對應 NFR-AVL）**:
```
【HA 架構設計】

1️⃣ Load Balancer
- Cloud Load Balancing (Global HTTP(S) Load Balancer)
- Health Check: GET /actuator/health (30s interval)
- Multi-Region 部署（asia-east1, asia-northeast1）
- 自動容錯移轉與流量分配

2️⃣ Application Layer
- Cloud Run (Serverless Containers)
- 自動擴展 (min: 2, max: 100 instances)
- CPU > 70% 或 Request Queue 自動擴展
- Rolling Update (Zero Downtime Deployment)
- 每個 Region 部署多個 instances

3️⃣ Database HA
- Cloud SQL for PostgreSQL (Enterprise HA)
- Multi-Regional Replication (Primary + Standby)
- Read Replicas (2 個，讀寫分離)
- Automatic Failover (RTO < 60s, RPO < 0)
- Point-in-Time Recovery (PITR) 7 天

4️⃣ Cache HA
- Memorystore for Redis (Standard Tier)
- Multi-Zone Replication (Primary + Replica)
- Automatic Failover (< 60s)
- 99.9% SLA

【SLA 計算】
- Cloud Load Balancing SLA: 99.99%
- Cloud Run SLA: 99.95% (multi-region)
- Cloud SQL SLA: 99.95% (HA configuration)
- Memorystore SLA: 99.9%
- **系統總 SLA: 99.79%** (超過 99.5% 目標)
```

---

### 階段 6-8: 實作開發（8-12 週）

#### 6.1 初始化專案結構

**輸入指令**:
```
請協助初始化 Spring Boot 和 Next.js 專案。

Backend 需求：
- Spring Boot 3.2 + Java 17
- Spring Security + JWT
- Spring Data JPA + Cloud SQL PostgreSQL
- Google Cloud Storage SDK
- Cloud Logging SDK
- Stripe SDK

Frontend 需求：
- Next.js 14 (App Router)
- TypeScript + Tailwind CSS
- Zustand (狀態管理)
- Axios (API 呼叫)
```

**AI 執行（建立檔案）**:
```
✅ 正在初始化 Backend...
✅ 正在初始化 Frontend...

【Backend 結構】
backend/
├── pom.xml
├── src/main/java/com/bnb/platform/
│   ├── BnbPlatformApplication.java
│   ├── config/
│   │   ├── SecurityConfig.java
│   │   ├── JwtConfig.java
│   │   └── CorsConfig.java
│   ├── controller/
│   │   ├── AuthController.java
│   │   ├── ListingController.java
│   │   ├── BookingController.java
│   │   └── PaymentController.java
│   ├── service/
│   │   ├── AuthService.java
│   │   ├── ListingService.java
│   │   ├── BookingService.java
│   │   └── PaymentService.java
│   ├── repository/
│   │   ├── UserRepository.java
│   │   ├── ListingRepository.java
│   │   └── BookingRepository.java
│   ├── entity/
│   │   ├── User.java
│   │   ├── Listing.java
│   │   └── Booking.java
│   └── dto/
│       ├── LoginRequest.java
│       ├── ListingResponse.java
│       └── BookingRequest.java
└── src/main/resources/
    └── application.yml

【Frontend 結構】
frontend/
├── package.json
├── next.config.js
├── tailwind.config.ts
├── app/
│   ├── layout.tsx
│   ├── page.tsx                    # 首頁
│   ├── search/page.tsx             # 搜尋頁
│   ├── listing/[id]/page.tsx       # 房源詳情
│   ├── booking/page.tsx            # 預訂頁
│   └── api/                        # API Routes
├── components/
│   ├── Header.tsx
│   ├── SearchBar.tsx
│   ├── ListingCard.tsx
│   └── BookingForm.tsx
├── lib/
│   ├── api.ts                      # Axios 配置
│   └── auth.ts                     # JWT 處理
└── stores/
    └── authStore.ts                # Zustand Store
```

#### 6.2 開發核心 API（範例）

**你的指令**:
```
請實作 US-001: 使用者可以搜尋房源

需求：
- API Endpoint: GET /api/listings/search
- Query Parameters: city, checkIn, checkOut, guests
- 回應格式: { listings: [], total: 0 }
- 包含 Controller, Service, Repository
```

**AI 產出**:
```java
// ListingController.java
@RestController
@RequestMapping("/api/listings")
public class ListingController {

    @Autowired
    private ListingService listingService;

    @GetMapping("/search")
    public ResponseEntity<SearchResponse> searchListings(
        @RequestParam String city,
        @RequestParam @DateTimeFormat(iso = ISO.DATE) LocalDate checkIn,
        @RequestParam @DateTimeFormat(iso = ISO.DATE) LocalDate checkOut,
        @RequestParam Integer guests
    ) {
        SearchRequest request = new SearchRequest(city, checkIn, checkOut, guests);
        SearchResponse response = listingService.searchListings(request);
        return ResponseEntity.ok(response);
    }
}

// ListingService.java
@Service
public class ListingService {

    @Autowired
    private ListingRepository listingRepository;

    @Autowired
    private BookingRepository bookingRepository;

    public SearchResponse searchListings(SearchRequest request) {
        // 1. 查詢該城市所有房源
        List<Listing> listings = listingRepository.findByCityAndMaxGuests(
            request.getCity(),
            request.getGuests()
        );

        // 2. 過濾已被預訂的房源
        List<Listing> availableListings = listings.stream()
            .filter(listing -> isAvailable(listing, request.getCheckIn(), request.getCheckOut()))
            .collect(Collectors.toList());

        // 3. 轉換為 DTO
        List<ListingDTO> dtos = availableListings.stream()
            .map(this::toDTO)
            .collect(Collectors.toList());

        return new SearchResponse(dtos, dtos.size());
    }

    private boolean isAvailable(Listing listing, LocalDate checkIn, LocalDate checkOut) {
        List<Booking> overlappingBookings = bookingRepository.findOverlappingBookings(
            listing.getId(),
            checkIn,
            checkOut
        );
        return overlappingBookings.isEmpty();
    }
}

// ListingRepository.java
@Repository
public interface ListingRepository extends JpaRepository<Listing, UUID> {

    @Query("SELECT l FROM Listing l WHERE " +
           "l.address->>'city' = :city AND " +
           "l.maxGuests >= :guests AND " +
           "l.status = 'ACTIVE'")
    List<Listing> findByCityAndMaxGuests(
        @Param("city") String city,
        @Param("guests") Integer guests
    );
}
```

#### 6.3 開發 Frontend 頁面（範例）

**你的指令**:
```
請實作 Next.js 搜尋頁面（/search）

需求：
- Server Component 進行 SSR
- 顯示搜尋結果列表
- 使用 Tailwind CSS
- 整合 Backend API
```

**AI 產出**:
```tsx
// app/search/page.tsx
import { ListingCard } from '@/components/ListingCard';
import { searchListings } from '@/lib/api';

interface SearchParams {
  city: string;
  checkIn: string;
  checkOut: string;
  guests: string;
}

export default async function SearchPage({
  searchParams,
}: {
  searchParams: SearchParams;
}) {
  // Server-side data fetching
  const { listings, total } = await searchListings({
    city: searchParams.city,
    checkIn: searchParams.checkIn,
    checkOut: searchParams.checkOut,
    guests: parseInt(searchParams.guests),
  });

  return (
    <div className="container mx-auto px-4 py-8">
      <h1 className="text-2xl font-bold mb-6">
        在 {searchParams.city} 找到 {total} 間房源
      </h1>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
        {listings.map((listing) => (
          <ListingCard key={listing.id} listing={listing} />
        ))}
      </div>

      {total === 0 && (
        <p className="text-center text-gray-500 mt-8">
          沒有找到符合條件的房源，請調整搜尋條件。
        </p>
      )}
    </div>
  );
}

// components/ListingCard.tsx
export function ListingCard({ listing }: { listing: Listing }) {
  return (
    <a href={`/listing/${listing.id}`} className="block hover:shadow-lg transition">
      <img
        src={listing.images[0]}
        alt={listing.title}
        className="w-full h-64 object-cover rounded-t-lg"
      />
      <div className="p-4">
        <h3 className="font-semibold text-lg">{listing.title}</h3>
        <p className="text-gray-600">{listing.propertyType} · {listing.bedrooms} 房</p>
        <p className="mt-2 font-bold">
          NT$ {listing.pricePerNight} / 晚
        </p>
      </div>
    </a>
  );
}

// lib/api.ts
export async function searchListings(params: SearchRequest) {
  const response = await fetch(
    `${process.env.NEXT_PUBLIC_API_URL}/api/listings/search?` +
    new URLSearchParams(params)
  );
  return response.json();
}
```

---

### 階段 9: 測試與驗證（2-3 週）

#### 9.1 效能測試

**輸入指令**:
```
請協助建立效能測試計畫（參考 Performance_Test_Plan_Template.md）

測試場景：
- 搜尋 API 負載測試（500 concurrent users）
- 預訂流程壓力測試（1500 concurrent users）
```

**AI 產出（JMeter 測試計畫）**:
```xml
<!-- SearchAPI_LoadTest.jmx -->
<TestPlan>
  <ThreadGroup name="Search API Load Test">
    <numThreads>500</numThreads>
    <rampUp>60</rampUp>
    <duration>1800</duration>

    <HTTPSampler name="Search Listings">
      <domain>api.bnb-platform.com</domain>
      <path>/api/listings/search</path>
      <method>GET</method>
      <parameters>
        <param name="city" value="台北市" />
        <param name="checkIn" value="2025-12-20" />
        <param name="checkOut" value="2025-12-22" />
        <param name="guests" value="2" />
      </parameters>
    </HTTPSampler>

    <ResultCollector>
      <AssertionResults>
        <responseTime>500ms</responseTime>  <!-- p95 < 500ms -->
        <successRate>99.9%</successRate>
      </AssertionResults>
    </ResultCollector>
  </ThreadGroup>
</TestPlan>
```

---

### 階段 10: Google Cloud 部署設定

#### 10.1 GCP 專案初始化

**輸入指令**:
```
請協助設定 Google Cloud Platform 部署環境。

需要建立：
1. Cloud SQL PostgreSQL instance (HA configuration)
2. Memorystore for Redis
3. Cloud Storage bucket
4. Cloud Run services
5. Cloud Load Balancing
```

**GCP 配置步驟**:
```bash
# 1. 設定 GCP 專案
gcloud config set project bnb-platform-prod

# 2. 建立 Cloud SQL PostgreSQL instance (HA)
gcloud sql instances create bnb-db-prod \
  --database-version=POSTGRES_15 \
  --tier=db-custom-4-16384 \
  --region=asia-east1 \
  --availability-type=REGIONAL \
  --storage-type=SSD \
  --storage-size=100GB \
  --backup-start-time=03:00 \
  --maintenance-window-day=SUN \
  --maintenance-window-hour=4

# 建立資料庫
gcloud sql databases create bnb_platform --instance=bnb-db-prod

# 建立使用者
gcloud sql users create bnb_user \
  --instance=bnb-db-prod \
  --password=STRONG_PASSWORD

# 3. 建立 Memorystore Redis instance
gcloud redis instances create bnb-cache-prod \
  --size=5 \
  --region=asia-east1 \
  --tier=standard \
  --redis-version=redis_7_0

# 4. 建立 Cloud Storage bucket (房源照片)
gsutil mb -l asia-east1 -c STANDARD gs://bnb-listing-images-prod/
gsutil iam ch allUsers:objectViewer gs://bnb-listing-images-prod/  # 公開讀取

# 5. 建立 Artifact Registry (Docker images)
gcloud artifacts repositories create bnb-services \
  --repository-format=docker \
  --location=asia-east1 \
  --description="BnB Platform Docker Images"
```

#### 10.2 Spring Boot 配置（GCP）

**application-gcp.yml**:
```yaml
spring:
  application:
    name: bnb-platform-api

  # Cloud SQL 配置
  datasource:
    url: jdbc:postgresql:///bnb_platform?cloudSqlInstance=bnb-platform-prod:asia-east1:bnb-db-prod&socketFactory=com.google.cloud.sql.postgres.SocketFactory
    username: bnb_user
    password: ${DB_PASSWORD}  # 從 Secret Manager 讀取
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  # Redis 配置 (Memorystore)
  redis:
    host: ${REDIS_HOST}  # Memorystore private IP
    port: 6379
    timeout: 2000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5

# Google Cloud Storage 配置
gcp:
  storage:
    bucket-name: bnb-listing-images-prod
    project-id: bnb-platform-prod

  # Cloud Logging 配置
  logging:
    enabled: true
    project-id: bnb-platform-prod

# Stripe 配置
stripe:
  api-key: ${STRIPE_SECRET_KEY}  # 從 Secret Manager 讀取
  webhook-secret: ${STRIPE_WEBHOOK_SECRET}

# 監控配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      stackdriver:
        enabled: true
        project-id: bnb-platform-prod
```

#### 10.3 Dockerfile (Cloud Run 優化)

**backend/Dockerfile**:
```dockerfile
# Stage 1: Build
FROM maven:3.9-eclipse-temurin-17 AS build
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline
COPY src ./src
RUN mvn clean package -DskipTests

# Stage 2: Runtime
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app

# 安裝 Cloud SQL Proxy (for local development)
RUN apk add --no-cache wget && \
    wget https://storage.googleapis.com/cloud-sql-connectors/cloud-sql-proxy/v2.7.0/cloud-sql-proxy.linux.amd64 -O cloud-sql-proxy && \
    chmod +x cloud-sql-proxy

COPY --from=build /app/target/*.jar app.jar

# Cloud Run 預設 PORT 環境變數
ENV PORT=8080
EXPOSE 8080

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=40s \
  CMD wget --no-verbose --tries=1 --spider http://localhost:8080/actuator/health || exit 1

# 啟動應用
ENTRYPOINT ["java", "-Xmx1g", "-Xms512m", "-jar", "app.jar", "--spring.profiles.active=gcp"]
```

#### 10.4 部署到 Cloud Run

```bash
# 1. 建立 Docker image
cd backend
docker build -t asia-east1-docker.pkg.dev/bnb-platform-prod/bnb-services/listing-service:v1.0.0 .

# 2. 推送到 Artifact Registry
docker push asia-east1-docker.pkg.dev/bnb-platform-prod/bnb-services/listing-service:v1.0.0

# 3. 部署到 Cloud Run (Listing Service)
gcloud run deploy listing-service \
  --image=asia-east1-docker.pkg.dev/bnb-platform-prod/bnb-services/listing-service:v1.0.0 \
  --platform=managed \
  --region=asia-east1 \
  --allow-unauthenticated \
  --min-instances=2 \
  --max-instances=100 \
  --cpu=2 \
  --memory=2Gi \
  --timeout=300 \
  --concurrency=80 \
  --set-env-vars="SPRING_PROFILES_ACTIVE=gcp" \
  --set-secrets="DB_PASSWORD=bnb-db-password:latest,STRIPE_SECRET_KEY=stripe-secret:latest" \
  --vpc-connector=bnb-vpc-connector \
  --service-account=bnb-backend-sa@bnb-platform-prod.iam.gserviceaccount.com

# 4. 設定 Cloud Load Balancing
gcloud compute backend-services create bnb-backend-service \
  --global \
  --load-balancing-scheme=EXTERNAL_MANAGED \
  --protocol=HTTPS

# 5. 新增 Cloud Run backend
gcloud compute backend-services add-backend bnb-backend-service \
  --global \
  --network-endpoint-group=listing-service-neg \
  --network-endpoint-group-region=asia-east1

# 6. 建立 URL Map
gcloud compute url-maps create bnb-url-map \
  --default-service=bnb-backend-service

# 7. 建立 HTTPS Load Balancer
gcloud compute target-https-proxies create bnb-https-proxy \
  --url-map=bnb-url-map \
  --ssl-certificates=bnb-ssl-cert

gcloud compute forwarding-rules create bnb-https-forwarding-rule \
  --global \
  --target-https-proxy=bnb-https-proxy \
  --ports=443
```

#### 10.5 Cloud Storage 整合 (圖片上傳)

**StorageService.java** (使用 GCS SDK):
```java
import com.google.cloud.storage.*;
import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

@Service
public class StorageService {

    private final Storage storage;
    private final String bucketName = "bnb-listing-images-prod";

    public StorageService() {
        this.storage = StorageOptions.getDefaultInstance().getService();
    }

    public String uploadListingImage(UUID listingId, MultipartFile file) throws IOException {
        String fileName = String.format("listings/%s/%s", listingId, file.getOriginalFilename());

        BlobId blobId = BlobId.of(bucketName, fileName);
        BlobInfo blobInfo = BlobInfo.newBuilder(blobId)
            .setContentType(file.getContentType())
            .setCacheControl("public, max-age=31536000")
            .build();

        storage.create(blobInfo, file.getBytes());

        // 回傳公開 URL
        return String.format("https://storage.googleapis.com/%s/%s", bucketName, fileName);
    }

    public void deleteListingImage(String gcsUrl) {
        String fileName = gcsUrl.replace("https://storage.googleapis.com/" + bucketName + "/", "");
        BlobId blobId = BlobId.of(bucketName, fileName);
        storage.delete(blobId);
    }
}
```

#### 10.6 監控與日誌

**Cloud Logging 整合**:
```xml
<!-- pom.xml -->
<dependency>
    <groupId>com.google.cloud</groupId>
    <artifactId>spring-cloud-gcp-starter-logging</artifactId>
</dependency>
```

**logback-spring.xml** (Structured Logging):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <springProfile name="gcp">
        <appender name="CLOUD" class="com.google.cloud.logging.logback.LoggingAppender">
            <log>application.log</log>
            <flushLevel>INFO</flushLevel>
        </appender>

        <root level="INFO">
            <appender-ref ref="CLOUD"/>
        </root>
    </springProfile>
</configuration>
```

**查看日誌**:
```bash
# 即時查看 Cloud Run 日誌
gcloud logging tail "resource.type=cloud_run_revision AND resource.labels.service_name=listing-service"

# 查詢錯誤日誌
gcloud logging read "resource.type=cloud_run_revision AND severity>=ERROR" --limit=50
```

#### 10.7 成本估算（Google Cloud）

| 服務 | 配置 | 月費用 (USD) | 年費用 (USD) |
|------|------|-------------|-------------|
| Cloud Run (4 services) | 2-100 instances, 2 vCPU, 2GB RAM | $600 | $7,200 |
| Cloud SQL PostgreSQL | db-custom-4-16384, HA, 100GB SSD | $450 | $5,400 |
| Memorystore Redis | Standard, 5GB | $150 | $1,800 |
| Cloud Storage | 500GB, 10TB egress | $100 | $1,200 |
| Cloud Load Balancing | 10M requests/month | $100 | $1,200 |
| Cloud Logging | 100GB/month | $50 | $600 |
| Cloud Monitoring | Standard metrics | $0 | $0 |
| **總計** | | **$1,450/月** | **$17,400/年** |

**3 年 TCO**: $52,200 (vs AWS $62k，節省 16%)

---

## 📊 完整開發時程參考

| 階段 | 時間 | 產出文檔/交付物 |
|------|------|----------------|
| Stage 1-2: 需求分析 | 2 週 | PRD, FRD, STRIDE 分析 |
| Stage 3: 技術選型 | 1 週 | Tech Stack Report, ADR, 成本估算 |
| Stage 4-5: 架構設計 | 2 週 | SRD, C4 Model, DB Schema, API Spec |
| Stage 6-8: 開發實作 | 12 週 | Frontend + Backend + Integration |
| Stage 9: 測試驗證 | 2 週 | 測試報告, 效能測試, 安全測試 |
| Stage 10: GCP 部署 | 1 週 | Cloud SQL, Cloud Run, Monitoring |
| **總計** | **20 週 (約 5 個月)** | **完整文檔 + Production-Ready 系統 + GCP 部署** |

---

## 🚀 快速開始指令（Copy & Paste）

```bash
# Step 1: 建立專案
mkdir BnB_Booking_Platform && cd BnB_Booking_Platform
mkdir -p {frontend,backend,docs,infra}

# Step 2: 複製 AISDLC
cp -r /Users/wuweihong/Cursor_Project/AISDLC_ALL/AISDLC_v0.09 ./AISDLC

# Step 3: 開啟 Cursor AI
# File → Open Folder → 選擇 BnB_Booking_Platform
```

**Cursor AI Chat 第一個指令**:
```
請載入 AISDLC Framework 並開始 Greenfield Web App 開發流程：

讀取：AISDLC/AISDLC_INIT.md

專案名稱：BnB Booking Platform
平台：Web App (前後端分離)
技術棧：Next.js 14 + Spring Boot 3 + Cloud SQL PostgreSQL
部署環境：Google Cloud Platform (Cloud Run + Cloud SQL + Cloud Storage)
目標：民宿預訂平台（房源搜尋、預訂、付款）
```

---

## 📚 參考文檔

- [Greenfield SOP](../../scenarios/greenfield/SOP.md)
- [Tech Stack Selection Matrix](../system/planning/Tech_Stack_Selection_Matrix.md)
- [Security Architecture Checklist](../system/architecture/Security_Architecture_Checklist.md)
- [High Availability Architecture Checklist](../system/architecture/High_Availability_Architecture_Checklist.md)
- [Performance Test Plan Template](../system/testing/Performance_Test_Plan_Template.md)

---

**維護記錄**:
- v0.09 (2025-12-13): 初版建立（民宿預訂網站完整範例，Google Cloud 部署版本）
  - 部署平台：Google Cloud Platform (Cloud Run + Cloud SQL + Cloud Storage + Memorystore)
  - 技術棧：Next.js 14 + Spring Boot 3.2 + PostgreSQL 15
  - 新增：階段 10 GCP 部署完整指南（含 gcloud CLI 命令、Spring Boot 配置、監控設定）
  - 成本估算：3 年 TCO $52,200（比 AWS 節省 16%）
