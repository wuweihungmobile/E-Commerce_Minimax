# AISDLC 第三方整合實戰範例：民宿預訂網站

**版本**: v0.09
**最後更新**: 2025-12-16
**情境**: 第三方整合 (Integration)
**平台**: Web (電子商務)
**技術棧**: React (Next.js) + Spring Boot + PostgreSQL + Google Cloud

---

## 📋 範例概述

本文檔展示如何使用 **AISDLC v0.09** 在 **Cursor AI** 環境中，透過 **Claude Code** 開發民宿預訂網站，涵蓋從專案設定到完整開發流程。

**技術棧**:
- **前端**: React 18 + Next.js 14 (App Router)
- **後端**: Spring Boot 3.2 + Java 17
- **資料庫**: PostgreSQL 15
- **部署**: Google Cloud (Cloud Run + Cloud SQL)
- **第三方整合**: Google Maps API, Stripe Payment, Firebase Auth

---

## 🎯 第一部分：環境設定

### 步驟 1.1: 設定 Cursor AI 專案路徑

**1.1.1 創建專案目錄**

```bash
# 創建 Monorepo 結構
mkdir -p ~/projects/BnBBooking
cd ~/projects/BnBBooking

# 創建前後端子專案
mkdir -p frontend backend
mkdir -p infrastructure/terraform

# 創建共用文檔目錄
mkdir -p docs/{requirements,design,api,tests}
```

**1.1.2 在 Cursor 開啟專案**

```bash
cursor ~/projects/BnBBooking
```

或：
1. 開啟 Cursor AI
2. File → Open Folder
3. 選擇 `~/projects/BnBBooking`

---

### 步驟 1.2: 安裝設定 AISDLC v0.09

**1.2.1 建立符號連結**

```bash
cd ~/projects/BnBBooking
ln -s ~/projects/AISDLC_ALL/AISDLC_v0.09 ./AISDLC

# 驗證
ls -la | grep AISDLC
```

**1.2.2 創建 CLAUDE.md**

```bash
cat > CLAUDE.md << 'EOF'
# BnBBooking 專案指引

**專案名稱**: BnBBooking (民宿預訂平台)
**AISDLC 版本**: v0.09
**開發情境**: 第三方整合 (Integration)

## 專案架構

- **前端**: Next.js 14 (App Router) + TypeScript + Tailwind CSS
- **後端**: Spring Boot 3.2 + JPA + PostgreSQL
- **部署**: Google Cloud Run + Cloud SQL
- **第三方服務**:
  - Google Maps API (地圖顯示、地址搜尋)
  - Stripe (金流處理)
  - Firebase Auth (使用者認證)
  - SendGrid (Email 通知)

## AISDLC 框架路徑

- **框架位置**: `./AISDLC/`
- **文檔輸出**: `./docs/`

## 開發規範

1. 前後端分離架構，透過 RESTful API 溝通
2. 所有需求文檔遵循 AISDLC PRD/FRD 模板
3. API 設計遵循 AISDLC API 規格模板
4. 使用 AISDLC Integration 情境 SOP

## Monorepo 結構

```
BnBBooking/
├── frontend/          # Next.js 專案
├── backend/           # Spring Boot 專案
├── infrastructure/    # Terraform IaC
├── docs/              # AISDLC 文檔
└── AISDLC/            # 符號連結
```

## 重要提醒

- 執行任何 workflow 前，先讀取 `AISDLC/AISDLC_INIT.md`
- 使用 Integration Scenario: `AISDLC/scenarios/integration/SOP.md`
- API 文檔必須使用 `AISDLC/docs_template/core/api/API_Specification_Template.md`
EOF
```

**1.2.3 初始化前後端專案**

```bash
# 前端 - Next.js
cd frontend
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir

# 後端 - Spring Boot (使用 Spring Initializr)
cd ../backend
# 或在 start.spring.io 下載後解壓縮至此目錄
```

**1.2.4 創建文檔目錄結構**

```bash
cd ~/projects/BnBBooking
mkdir -p docs/{requirements,design,api,tests}
mkdir -p docs/{analysis,planning,reports}
```

---

## 🚀 第二部分：使用 Claude Code 開發

### 步驟 2.1: 啟動 Claude Code

**2.1.1 在 Cursor 終端機啟動**

```bash
claude code
```

**2.1.2 驗證 AISDLC 載入**

💬 **您對 Claude 說**：
```
請讀取 AISDLC/AISDLC_INIT.md 確認框架已載入
```

---

### 步驟 2.2: 第一階段 - 需求分析

**2.2.1 初始化專案並載入 Workflow**

💬 **您對 Claude 說**：
```
我要開發民宿預訂平台 (BnBBooking)。

技術棧:
- 前端: Next.js 14 + TypeScript
- 後端: Spring Boot 3.2 + PostgreSQL
- 部署: Google Cloud

第三方整合:
- Google Maps API (地圖)
- Stripe (金流)
- Firebase Auth (登入)
- SendGrid (Email)

請執行:
1. 讀取 AISDLC/AISDLC_INIT.md
2. 讀取 AISDLC/scenarios/integration/SOP.md
3. 建議開發流程
```

🤖 **Claude 回應範例**：
```
已讀取 AISDLC_INIT.md 和 Integration SOP。

建議流程:
1. ✅ unified-requirements-extraction (需求萃取)
2. ✅ user-story-and-design (使用者故事與設計)
3. ✅ api-specification-generation (API 規格)
4. ✅ frontend-backend-interaction-analysis (前後端互動分析)
5. ✅ document-consistency-check (一致性驗證)

請提供功能需求清單，我將開始需求萃取。
```

---

**2.2.2 執行需求萃取**

💬 **您對 Claude 說**：
```
請使用 unified-requirements-extraction workflow 分析以下需求:

## 民宿預訂平台功能需求

### 核心功能
1. 使用者管理
   - 房東註冊/登入 (Firebase Auth)
   - 房客註冊/登入 (Firebase Auth + Google/Facebook)
   - 個人資料管理

2. 房源管理 (房東)
   - 新增房源 (整合 Google Maps 定位)
   - 上傳房源照片 (Google Cloud Storage)
   - 設定房間資訊 (價格、設施、房規)
   - 管理可訂日期與價格策略

3. 搜尋與瀏覽 (房客)
   - 關鍵字搜尋 (地點、日期、人數)
   - 地圖檢視 (Google Maps API)
   - 篩選條件 (價格區間、設施、評分)
   - 房源詳情頁面

4. 預訂流程
   - 選擇日期與人數
   - 價格計算 (含清潔費、服務費)
   - 線上付款 (Stripe)
   - 訂單確認 Email (SendGrid)

5. 訂單管理
   - 房客查看訂單
   - 房東接受/拒絕訂單
   - 取消政策處理
   - 退款流程 (Stripe Refund)

6. 評價系統
   - 房客評價房源
   - 房東評價房客
   - 雙向評價機制

### 非功能需求
- 響應式設計 (RWD)
- 多語系支援 (繁中/英文)
- SEO 優化 (Next.js SSR)
- 效能: 首頁載入 < 2 秒
- 安全: HTTPS, CSRF 保護, SQL Injection 防護

請載入對應 Agents 並開始分析。
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 AISDLC/workflow/core/unified-requirements-extraction.md
步驟 2: 載入 Agents:
  - SA: AISDLC/agent/core/04.sa-analyst-zh.yaml
  - BA: AISDLC/agent/core/02.ba-business-analyst-zh.yaml

步驟 3: 🔴 需求釐清問題

【商業邏輯確認】
Q1: 預訂流程 - 是「即時確認」還是「房東審核」模式？
Q2: 付款時機 - 預訂時全額付款？還是訂金制？
Q3: 取消政策 - 標準政策為何？(靈活/中等/嚴格)
Q4: 服務費 - 由誰負擔？房客/房東/雙方分攤？

【技術整合確認】
Q5: Google Maps - 需要路線規劃功能嗎？
Q6: Stripe - 支援哪些幣別？(TWD/USD/EUR?)
Q7: Firebase Auth - 需要手機號碼驗證嗎？
Q8: 多語系 - 使用 i18n 還是資料庫儲存？

【資料範圍確認】
Q9: 初期目標區域？(台灣/全球)
Q10: 預估房源數量級別？(百/千/萬)

請回答以上問題。
```

---

**2.2.3 回答需求釐清問題**

💬 **您對 Claude 說**：
```
A1: 即時確認模式 (類似 Airbnb)
A2: 預訂時全額付款，款項先由平台保管，退房後 24 小時撥款給房東
A3: 標準政策 - 入住前 7 天免費取消，7 天內取消收 50% 手續費
A4: 服務費由房客支付 (訂單金額的 10%)
A5: 僅需地圖標記與地址搜尋，不需路線規劃
A6: 初期支援 TWD, USD
A7: Email 驗證即可，手機驗證列為 v2.0
A8: 使用 next-i18next
A9: 初期僅台灣
A10: 預估 500-1000 房源
```

🤖 **Claude 執行**：
```
收到！生成 FRD...

步驟 1: 讀取 AISDLC/docs_template/core/frd/FRD_Universal_Template.md
步驟 2: 讀取 AISDLC/guides/system/naming/AISDLC_ID_Naming_Convention.md

【生成中...】

✅ 完成！已生成:
- docs/requirements/FRD_BnBBooking_v1.0.md

內容包含:
- 15 個 User Stories (US-001 ~ US-015)
  - US-001: 房客使用 Email 註冊
  - US-002: 房客使用 Google 登入
  - US-003: 房東新增房源並整合 Google Maps
  - US-004: 房客搜尋房源 (關鍵字+地圖)
  - US-005: 房客預訂並使用 Stripe 付款
  - US-006: 系統發送訂單確認 Email (SendGrid)
  - ...
- 68 個 Acceptance Criteria (AC-001-1 ~ AC-015-N)
- 第三方整合需求清單 (Integration Requirements)

請檢閱 FRD。
```

---

### 步驟 2.3: 第二階段 - 系統設計

**2.3.1 生成 SRD (系統需求文檔)**

💬 **您對 Claude 說**：
```
FRD 已確認。請執行 user-story-and-design workflow，生成 SRD 包含:

1. 系統架構設計 (前後端分離 + 微服務考量)
2. 資料庫 Schema (PostgreSQL)
3. 前端架構 (Next.js App Router + 狀態管理)
4. 後端架構 (Spring Boot + 分層架構)
5. 第三方整合架構圖
6. 部署架構 (Google Cloud)
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 AISDLC/workflow/core/user-story-and-design.md
步驟 2: 載入 SD Agent
  - AISDLC/agent/core/05.sd-architect-zh.yaml

步驟 3: 讀取架構指南
  - AISDLC/guides/system/architecture/C4_Model_Guidelines.md
  - AISDLC/guides/system/architecture/Web_Architecture_Decision_Tree.md

【設計中...】

✅ 完成！已生成:

1. docs/design/SRD_BnBBooking_v1.0.md
   - C4 Model 架構圖 (Context/Container/Component)
   - 系統架構圖 (前後端分離 + Google Cloud)
   - 資料庫 ER Diagram (12 張表)
   - 前端元件樹 (Next.js Pages/Components)
   - 後端分層架構 (Controller/Service/Repository)

2. docs/design/Database_Schema.md
   主要資料表:
   - users (使用者)
   - properties (房源)
   - rooms (房間)
   - bookings (訂單)
   - payments (付款記錄)
   - reviews (評價)
   - amenities (設施)
   - property_photos (照片)
   - availability (可訂日期)
   - pricing_rules (價格策略)
   - cancellation_policies (取消政策)
   - notifications (通知記錄)

3. docs/design/Integration_Architecture.md
   第三方整合設計:
   - Firebase Auth 整合流程圖
   - Google Maps API 使用場景
   - Stripe Payment Flow (含 Webhook)
   - SendGrid Email Templates

4. docs/design/Architecture_Decision_Records.md
   - ADR-001: 選擇 Next.js 14 App Router (SEO + SSR)
   - ADR-002: 選擇 PostgreSQL (關聯式資料 + ACID)
   - ADR-003: 選擇 Google Cloud Run (無伺服器 + 自動擴展)
   - ADR-004: 選擇 Stripe (PCI 合規 + 多幣別)

請檢閱設計文檔。
```

---

**2.3.2 生成 API 規格**

💬 **您對 Claude 說**：
```
設計文檔已確認。請執行 api-specification-generation workflow，
生成完整的 RESTful API 規格，包含所有 CRUD 端點和第三方整合端點。
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 AISDLC/workflow/core/api-specification-generation.md
步驟 2: 讀取 API 模板與指南
  - AISDLC/docs_template/core/api/API_Specification_Template.md
  - AISDLC/guides/system/api/API_Versioning_Guide.md

【生成中...】

✅ 完成！已生成 28 個 API 規格文檔:

docs/api/
├── API_Index.md
│
├── Auth/ (認證 - 3 個)
│   ├── API_Auth_Register.md           # POST /api/v1/auth/register
│   ├── API_Auth_Login.md              # POST /api/v1/auth/login
│   └── API_Auth_Firebase_Callback.md  # POST /api/v1/auth/firebase/callback
│
├── Properties/ (房源 - 6 個)
│   ├── API_Properties_Create.md       # POST /api/v1/properties
│   ├── API_Properties_List.md         # GET /api/v1/properties
│   ├── API_Properties_Detail.md       # GET /api/v1/properties/{id}
│   ├── API_Properties_Update.md       # PUT /api/v1/properties/{id}
│   ├── API_Properties_Delete.md       # DELETE /api/v1/properties/{id}
│   └── API_Properties_Search.md       # POST /api/v1/properties/search
│
├── Bookings/ (訂單 - 5 個)
│   ├── API_Bookings_Create.md         # POST /api/v1/bookings
│   ├── API_Bookings_List.md           # GET /api/v1/bookings
│   ├── API_Bookings_Detail.md         # GET /api/v1/bookings/{id}
│   ├── API_Bookings_Cancel.md         # POST /api/v1/bookings/{id}/cancel
│   └── API_Bookings_Calculate.md      # POST /api/v1/bookings/calculate
│
├── Payments/ (付款 - 4 個)
│   ├── API_Payments_Stripe_Intent.md  # POST /api/v1/payments/stripe/intent
│   ├── API_Payments_Stripe_Confirm.md # POST /api/v1/payments/stripe/confirm
│   ├── API_Payments_Stripe_Webhook.md # POST /api/v1/payments/stripe/webhook
│   └── API_Payments_Refund.md         # POST /api/v1/payments/{id}/refund
│
├── Reviews/ (評價 - 3 個)
│   ├── API_Reviews_Create.md          # POST /api/v1/reviews
│   ├── API_Reviews_List.md            # GET /api/v1/reviews
│   └── API_Reviews_Property.md        # GET /api/v1/properties/{id}/reviews
│
├── Maps/ (地圖 - 2 個)
│   ├── API_Maps_Geocoding.md          # POST /api/v1/maps/geocoding
│   └── API_Maps_PlaceSearch.md        # POST /api/v1/maps/search
│
└── Notifications/ (通知 - 2 個)
    ├── API_Notifications_Send.md      # POST /api/v1/notifications/send
    └── API_Notifications_Templates.md # GET /api/v1/notifications/templates

每個 API 規格包含:
- 完整 Request/Response 範例 (JSON)
- JWT 認證規範
- 錯誤碼定義 (400, 401, 403, 404, 500)
- Rate Limiting (100 req/min)
- 第三方 API 整合說明 (Stripe, Google Maps, SendGrid)

請檢閱 API 規格。
```

---

**2.3.3 前後端互動分析**

💬 **您對 Claude 說**：
```
請執行 frontend-backend-interaction-analysis workflow，
分析前端頁面與後端 API 的對應關係，生成互動流程圖。
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 AISDLC/workflow/core/frontend-backend-interaction-analysis.md

【分析中...】

✅ 完成！已生成:

docs/design/Frontend_Backend_Interaction.md

內容包含:
1. 頁面-API 對應表
   - 首頁 (/) → API_Properties_Search, API_Properties_List
   - 房源詳情 (/properties/[id]) → API_Properties_Detail, API_Reviews_Property
   - 預訂頁 (/booking/[id]) → API_Bookings_Calculate, API_Payments_Stripe_Intent
   - 訂單管理 (/bookings) → API_Bookings_List

2. 關鍵流程序列圖 (Sequence Diagrams)
   - 使用者登入流程 (Firebase Auth → Backend JWT)
   - 房源搜尋流程 (前端 → Backend → Google Maps API)
   - 預訂付款流程 (前端 → Backend → Stripe → Webhook → SendGrid)
   - 取消退款流程 (前端 → Backend → Stripe Refund)

3. 狀態管理設計
   - 使用 Zustand 管理全域狀態
   - Auth Store (使用者狀態)
   - Booking Store (預訂流程狀態)
   - Search Store (搜尋篩選條件)

4. 錯誤處理策略
   - API 錯誤統一處理 (axios interceptor)
   - 使用者友善錯誤訊息對照表
   - 重試機制 (網路錯誤自動重試 3 次)

請檢閱互動設計。
```

---

### 步驟 2.4: 第三階段 - 一致性驗證

**2.4.1 執行文檔一致性檢查**

💬 **您對 Claude 說**：
```
請執行 document-consistency-check workflow，
驗證 FRD、SRD、API 規格、前後端互動文檔的一致性。
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 AISDLC/workflow/core/document-consistency-check.md
步驟 2: 載入 QA Agent
  - AISDLC/agent/core/07.qa-tester-zh.yaml

【驗證中...】

✅ 驗證結果:

一致性檢查: 15 個 User Stories × 4 層文檔 = 60 個檢查點

通過: 56/60 (93%)
問題: 4 個

🔴 問題 1: US-010 (房東查看收益報表) 缺少對應 API
  - FRD: ✅ 有定義
  - SRD: ✅ 有設計
  - API: ❌ 缺少 API_Reports_Revenue.md
  - 互動: ❌ 缺少頁面-API 對應

🔴 問題 2: API_Maps_PlaceSearch.md 未在 SRD 技術選型說明
  - 建議: 在 SRD 增加 Google Places API 選型理由

🔴 問題 3: Database Schema 缺少 `user_preferences` 表
  - FRD US-012 提到「使用者偏好設定」
  - 但 Database_Schema.md 未定義對應資料表

🔴 問題 4: Stripe Webhook 端點的安全驗證未在 API 規格說明
  - API_Payments_Stripe_Webhook.md 需補充簽章驗證步驟

是否修正這些問題？(是/否)
```

💬 **您對 Claude 說**：
```
是，請全部修正。
```

🤖 **Claude 執行**：
```
✅ 已修正！

1. 新增 docs/api/Reports/API_Reports_Revenue.md
2. 更新 docs/design/SRD_BnBBooking_v1.0.md (新增 Google Places API 說明)
3. 更新 docs/design/Database_Schema.md (新增 user_preferences 表)
4. 更新 docs/api/Payments/API_Payments_Stripe_Webhook.md (補充 Webhook 簽章驗證)

✅ 一致性驗證通過！(60/60)

生成報告: docs/reports/Document_Consistency_Report_2025-12-16.md
```

---

### 步驟 2.5: 第四階段 - 程式碼實作

**2.5.1 生成後端程式碼架構 (Spring Boot)**

💬 **您對 Claude 說**：
```
文檔已完成。請根據 SRD 生成 Spring Boot 後端程式碼架構:

1. 生成分層架構 (Controller/Service/Repository/Entity)
2. 生成 JPA Entity (對應 Database Schema)
3. 生成 Security Config (JWT + Spring Security)
4. 生成第三方整合 Service (Stripe, Google Maps, SendGrid)

請遵循 Spring Boot 最佳實踐。
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 docs/design/SRD_BnBBooking_v1.0.md
步驟 2: 讀取 docs/design/Database_Schema.md
步驟 3: 讀取 AISDLC/scenarios/integration/SOP.md

【生成中...】

✅ 完成！已生成後端架構:

backend/src/main/java/com/bnbbooking/
├── config/
│   ├── SecurityConfig.java           # Spring Security + JWT
│   ├── JpaConfig.java                # JPA 設定
│   ├── StripeConfig.java             # Stripe 配置
│   └── GoogleMapsConfig.java         # Google Maps 配置
│
├── controller/
│   ├── AuthController.java           # 認證端點
│   ├── PropertyController.java       # 房源 CRUD
│   ├── BookingController.java        # 訂單管理
│   ├── PaymentController.java        # 付款與 Webhook
│   ├── ReviewController.java         # 評價
│   └── MapController.java            # 地圖相關
│
├── service/
│   ├── AuthService.java
│   ├── PropertyService.java
│   ├── BookingService.java
│   ├── PaymentService.java
│   ├── ReviewService.java
│   ├── integration/
│   │   ├── StripeIntegrationService.java
│   │   ├── GoogleMapsService.java
│   │   ├── FirebaseAuthService.java
│   │   └── SendGridEmailService.java
│   └── NotificationService.java
│
├── repository/
│   ├── UserRepository.java
│   ├── PropertyRepository.java
│   ├── BookingRepository.java
│   ├── PaymentRepository.java
│   └── ReviewRepository.java
│
├── entity/
│   ├── User.java
│   ├── Property.java
│   ├── Room.java
│   ├── Booking.java
│   ├── Payment.java
│   ├── Review.java
│   ├── Amenity.java
│   ├── PropertyPhoto.java
│   ├── Availability.java
│   ├── PricingRule.java
│   └── CancellationPolicy.java
│
├── dto/
│   ├── request/
│   │   ├── PropertyCreateRequest.java
│   │   ├── BookingCreateRequest.java
│   │   └── PaymentIntentRequest.java
│   └── response/
│       ├── PropertyDetailResponse.java
│       ├── BookingResponse.java
│       └── PaymentResponse.java
│
├── security/
│   ├── JwtTokenProvider.java
│   ├── JwtAuthenticationFilter.java
│   └── UserPrincipal.java
│
└── exception/
    ├── GlobalExceptionHandler.java
    ├── ResourceNotFoundException.java
    └── PaymentException.java

已生成所有檔案的基礎結構和介面定義。

下一步：生成前端架構？(是/否)
```

---

**2.5.2 生成前端程式碼架構 (Next.js)**

💬 **您對 Claude 說**：
```
是。請生成 Next.js 14 前端架構:

1. App Router 頁面結構
2. Zustand 狀態管理
3. API Client (axios)
4. UI 元件庫 (shadcn/ui)
5. 第三方整合 (Firebase Auth, Stripe, Google Maps)
```

🤖 **Claude 執行**：
```
【生成中...】

✅ 完成！已生成前端架構:

frontend/
├── app/
│   ├── layout.tsx                    # 根佈局 (含 i18n)
│   ├── page.tsx                      # 首頁 (房源搜尋)
│   ├── properties/
│   │   ├── [id]/
│   │   │   └── page.tsx              # 房源詳情
│   │   └── new/
│   │       └── page.tsx              # 新增房源
│   ├── booking/
│   │   └── [id]/
│   │       └── page.tsx              # 預訂頁面
│   ├── bookings/
│   │   └── page.tsx                  # 訂單列表
│   ├── auth/
│   │   ├── login/
│   │   │   └── page.tsx              # 登入
│   │   └── register/
│   │       └── page.tsx              # 註冊
│   └── profile/
│       └── page.tsx                  # 使用者資料
│
├── components/
│   ├── ui/                           # shadcn/ui 元件
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   ├── dialog.tsx
│   │   └── ... (其他 UI 元件)
│   ├── property/
│   │   ├── PropertyCard.tsx
│   │   ├── PropertyGallery.tsx
│   │   ├── PropertyAmenities.tsx
│   │   └── PropertyMap.tsx           # Google Maps 整合
│   ├── booking/
│   │   ├── DateRangePicker.tsx
│   │   ├── GuestSelector.tsx
│   │   ├── PriceCalculator.tsx
│   │   └── PaymentForm.tsx           # Stripe Elements
│   ├── auth/
│   │   ├── LoginForm.tsx
│   │   ├── RegisterForm.tsx
│   │   └── SocialLoginButtons.tsx    # Firebase Auth
│   └── layout/
│       ├── Header.tsx
│       ├── Footer.tsx
│       └── SearchBar.tsx
│
├── lib/
│   ├── api/
│   │   ├── client.ts                 # Axios 實例 (含 JWT interceptor)
│   │   ├── auth.ts                   # Auth API
│   │   ├── properties.ts             # Properties API
│   │   ├── bookings.ts               # Bookings API
│   │   ├── payments.ts               # Payments API
│   │   └── reviews.ts                # Reviews API
│   ├── firebase/
│   │   └── auth.ts                   # Firebase Auth 初始化
│   ├── stripe/
│   │   └── client.ts                 # Stripe.js 初始化
│   ├── google-maps/
│   │   └── loader.ts                 # Google Maps Loader
│   └── utils/
│       ├── formatters.ts             # 日期、金額格式化
│       └── validators.ts             # 表單驗證
│
├── store/
│   ├── authStore.ts                  # Zustand - 認證狀態
│   ├── bookingStore.ts               # Zustand - 預訂流程
│   ├── searchStore.ts                # Zustand - 搜尋篩選
│   └── index.ts
│
├── types/
│   ├── property.ts
│   ├── booking.ts
│   ├── user.ts
│   └── api.ts
│
├── hooks/
│   ├── useAuth.ts
│   ├── useProperties.ts
│   ├── useBookings.ts
│   └── useGoogleMaps.ts
│
└── middleware.ts                     # Next.js 中介軟體 (認證檢查)

已生成所有檔案的基礎結構。

下一步：實作核心功能？(是/否)
```

---

**2.5.3 實作核心功能 - 預訂付款流程**

💬 **您對 Claude 說**：
```
是。請實作「預訂付款流程」(US-005)，這是最核心的功能，包含:

後端:
1. BookingController.createBooking()
2. BookingService (業務邏輯 + 價格計算)
3. StripeIntegrationService (Payment Intent)
4. Stripe Webhook 處理
5. SendGridEmailService (確認信)

前端:
1. app/booking/[id]/page.tsx
2. PaymentForm.tsx (Stripe Elements)
3. bookingStore.ts (預訂流程狀態)

請包含完整的錯誤處理和測試。
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 docs/requirements/FRD_BnBBooking_v1.0.md (US-005)
步驟 2: 讀取 docs/api/Bookings/API_Bookings_Create.md
步驟 3: 讀取 docs/api/Payments/API_Payments_Stripe_Intent.md

【實作中...】

✅ 完成！已實作預訂付款流程:

【後端】
1. ✅ BookingController.java
   - POST /api/v1/bookings
   - 驗證: 日期可用性、房客身份
   - 觸發: 價格計算、Payment Intent 創建

2. ✅ BookingService.java
   - 價格計算邏輯 (房價 × 晚數 + 清潔費 + 服務費)
   - 可訂性檢查 (避免重複預訂)
   - 訂單狀態管理 (PENDING → CONFIRMED → COMPLETED)

3. ✅ StripeIntegrationService.java
   - createPaymentIntent() - 創建 Stripe Payment Intent
   - handleWebhook() - 處理 payment_intent.succeeded 事件
   - Webhook 簽章驗證 (Stripe Signature)

4. ✅ PaymentWebhookController.java
   - POST /api/v1/payments/stripe/webhook
   - 事件處理: payment_intent.succeeded → 更新訂單狀態 → 發送確認信

5. ✅ SendGridEmailService.java
   - sendBookingConfirmation() - 使用 SendGrid Template
   - 動態參數: 房源名稱、入住日期、總金額

【前端】
1. ✅ app/booking/[id]/page.tsx
   - SSR 載入房源資料
   - 日期範圍選擇器 (react-day-picker)
   - 即時價格計算
   - Stripe Elements 整合

2. ✅ components/booking/PaymentForm.tsx
   - Stripe CardElement
   - 表單驗證 (姓名、Email、付款資訊)
   - 載入狀態處理
   - 錯誤訊息顯示

3. ✅ store/bookingStore.ts
   - 狀態: selectedDates, guestCount, totalPrice, paymentStatus
   - Actions: setDates, calculatePrice, submitBooking

【測試】
1. ✅ 後端單元測試
   - BookingServiceTest.java
   - StripeIntegrationServiceTest.java (使用 Stripe Mock)

2. ✅ 前端測試
   - PaymentForm.test.tsx (React Testing Library)

【錯誤處理】
- 付款失敗 → 顯示友善錯誤訊息 + 重試選項
- 日期衝突 → 即時檢查並提示
- 網路錯誤 → 自動重試 3 次

【安全性】
- Stripe Payment Intent (PCI 合規)
- Webhook 簽章驗證
- CSRF Token
- Rate Limiting (每分鐘最多 10 次預訂請求)

程式碼已標註 AISDLC ID:
// US-005: 房客預訂並使用 Stripe 付款
// AC-005-1: 選擇日期並計算總價
// AC-005-2: 使用 Stripe 安全付款
// AC-005-3: 收到確認 Email

下一步：執行測試？(是/否)
```

---

### 步驟 2.6: 第五階段 - 測試驗證

**2.6.1 執行測試**

💬 **您對 Claude 說**：
```
是，請執行後端和前端測試。
```

🤖 **Claude 執行**：
```
【後端測試】
$ cd backend && ./mvnw test

✅ 測試結果:
BookingServiceTest
  ✅ testCreateBooking_Success (0.15s)
  ✅ testCreateBooking_DateConflict (0.08s)
  ✅ testCalculatePrice_Standard (0.05s)
  ✅ testCalculatePrice_WithCleaningFee (0.06s)

StripeIntegrationServiceTest
  ✅ testCreatePaymentIntent_Success (0.12s)
  ✅ testHandleWebhook_PaymentSucceeded (0.09s)
  ✅ testWebhookSignatureValidation (0.08s)

總計: 7 個測試通過，0 個失敗
覆蓋率: 89%

【前端測試】
$ cd frontend && npm test

✅ 測試結果:
PaymentForm.test.tsx
  ✅ renders payment form correctly (0.05s)
  ✅ validates required fields (0.08s)
  ✅ handles Stripe payment submission (0.12s)
  ✅ displays error on payment failure (0.09s)

總計: 4 個測試通過，0 個失敗

【整合測試】(需手動執行)
1. 使用 Stripe Test Card (4242 4242 4242 4242)
2. 完整預訂流程測試
3. Webhook 模擬 (stripe listen --forward-to localhost:8080/api/v1/payments/stripe/webhook)

生成測試報告:
- docs/tests/Unit_Test_Report_Booking_2025-12-16.md
- docs/tests/Integration_Test_Report_Stripe_2025-12-16.md
```

---

**2.6.2 生成驗收測試**

💬 **您對 Claude 說**：
```
測試通過。請根據 FRD 的 AC 生成驗收測試文檔。
```

🤖 **Claude 執行**：
```
步驟 1: 讀取 docs/requirements/FRD_BnBBooking_v1.0.md
步驟 2: 讀取 AISDLC/docs_template/core/tests/Acceptance_Test_Template.md

✅ 完成！已生成:

docs/tests/AT_US-005_BookingPayment.md

驗收測試案例:
AT-005-1: 房客可成功預訂並付款
  - 前置條件: 房客已登入，房源可訂
  - 測試步驟:
    1. 選擇入住日期 2025-03-01 ~ 2025-03-03 (2 晚)
    2. 系統顯示價格明細 (房價 $3000×2 + 清潔費 $500 + 服務費 $650 = $7150)
    3. 填寫 Stripe 測試卡號 4242 4242 4242 4242
    4. 點擊「確認付款」
  - 預期結果:
    ✅ 付款成功
    ✅ 訂單狀態變為 CONFIRMED
    ✅ 收到 SendGrid 確認信
    ✅ 房東收到新訂單通知
  - 實際結果: ✅ 通過 (2025-12-16 測試)

AT-005-2: 拒絕無效的信用卡
  - 測試卡號: 4000 0000 0000 0002 (Stripe 測試卡 - 拒絕)
  - 預期結果: 顯示錯誤「您的卡片被拒絕，請使用其他付款方式」
  - 實際結果: ✅ 通過

AT-005-3: 處理日期衝突
  - 測試步驟: 選擇已被預訂的日期
  - 預期結果: 即時顯示「此日期已被預訂，請選擇其他日期」
  - 實際結果: ✅ 通過

AT-005-4: Webhook 觸發確認信
  - 測試步驟: 使用 Stripe CLI 模擬 webhook
  - 預期結果: 確認信在 10 秒內送達
  - 實際結果: ✅ 通過 (平均 3.5 秒)

驗收測試狀態: 4/4 通過 (100%)
```

---

### 步驟 2.7: 第六階段 - 部署準備

**2.7.1 生成 Google Cloud 部署配置**

💬 **您對 Claude 說**：
```
請生成 Google Cloud 部署配置:

1. Dockerfile (前端 + 後端)
2. Cloud Run 部署腳本
3. Cloud SQL (PostgreSQL) 設定
4. Terraform IaC
5. 環境變數管理 (Secret Manager)
```

🤖 **Claude 執行**：
```
【生成中...】

✅ 完成！已生成部署配置:

infrastructure/
├── terraform/
│   ├── main.tf                       # 主配置
│   ├── variables.tf                  # 變數定義
│   ├── outputs.tf                    # 輸出
│   ├── cloud_run.tf                  # Cloud Run 服務
│   ├── cloud_sql.tf                  # PostgreSQL 資料庫
│   ├── secret_manager.tf             # 環境變數
│   └── vpc.tf                        # VPC 網路
│
├── docker/
│   ├── backend/
│   │   └── Dockerfile                # Spring Boot Dockerfile
│   └── frontend/
│       └── Dockerfile                # Next.js Dockerfile
│
└── scripts/
    ├── deploy-backend.sh             # 後端部署腳本
    ├── deploy-frontend.sh            # 前端部署腳本
    └── setup-secrets.sh              # 設定 Secret Manager

環境變數清單:
- DATABASE_URL (Cloud SQL)
- JWT_SECRET
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET
- GOOGLE_MAPS_API_KEY
- SENDGRID_API_KEY
- FIREBASE_CONFIG

部署步驟:
1. terraform init && terraform apply
2. ./scripts/setup-secrets.sh
3. ./scripts/deploy-backend.sh
4. ./scripts/deploy-frontend.sh

已生成文檔: docs/deployment/Google_Cloud_Deployment_Guide.md
```

---

## 📊 第三部分：完整開發流程總結

### 3.1 命令範例清單

#### 階段 1: 需求分析
```
命令 1: 讀取 AISDLC/AISDLC_INIT.md 和 Integration SOP
命令 2: 使用 unified-requirements-extraction 分析需求
命令 3: 回答 SA/BA Agents 的釐清問題
```

#### 階段 2: 系統設計
```
命令 4: 使用 user-story-and-design 生成 SRD
命令 5: 使用 api-specification-generation 生成 API 規格
命令 6: 使用 frontend-backend-interaction-analysis 分析互動
```

#### 階段 3: 一致性驗證
```
命令 7: 執行 document-consistency-check
命令 8: 修正一致性問題
```

#### 階段 4: 程式碼實作
```
命令 9: 生成後端架構 (Spring Boot)
命令 10: 生成前端架構 (Next.js)
命令 11: 實作核心功能 (預訂付款流程)
```

#### 階段 5: 測試驗證
```
命令 12: 執行單元測試與整合測試
命令 13: 生成驗收測試文檔
```

#### 階段 6: 部署準備
```
命令 14: 生成 Google Cloud 部署配置
```

---

### 3.2 開發時間軸

| 時間 | 階段 | 產出 |
|------|------|------|
| Week 1 | 需求分析 + 設計 | FRD, SRD, API 規格 (28 個) |
| Week 2 | 後端開發 | Spring Boot 完整架構 + 整合 |
| Week 3 | 前端開發 | Next.js 完整架構 + UI |
| Week 4 | 測試 + 部署 | 測試報告 + Google Cloud 部署 |

---

### 3.3 關鍵成功要素

#### ✅ DO
1. **明確指定 Workflow** - 例：「使用 api-specification-generation workflow」
2. **提供完整技術棧** - 前後端、資料庫、部署平台、第三方服務
3. **回答 Agent 問題** - 詳細回答商業邏輯和技術決策
4. **驗證文檔一致性** - 執行 document-consistency-check
5. **標註 AISDLC ID** - 程式碼註解標示 US-XXX, AC-XXX-X
6. **測試第三方整合** - 使用測試環境 (Stripe Test Mode, Google Maps Test API Key)

#### ❌ DON'T
1. **不跳過文檔階段** - 先完成 FRD/SRD 再寫程式碼
2. **不忽略安全性** - Webhook 簽章驗證、JWT、HTTPS
3. **不忽略錯誤處理** - 第三方 API 失敗時的降級方案
4. **不在生產環境測試** - 使用 Stripe Test Mode
5. **不硬編碼 API Key** - 使用環境變數 + Secret Manager

---

## 🎓 第四部分：第三方整合重點

### 4.1 Firebase Auth 整合

💬 **命令範例**：
```
請實作 Firebase Auth 整合 (US-002):

1. 後端: FirebaseAuthService.java (驗證 ID Token)
2. 前端: lib/firebase/auth.ts (Google/Facebook 登入)
3. JWT 轉換: Firebase Token → Backend JWT
4. 錯誤處理: Token 過期、驗證失敗

請包含安全最佳實踐。
```

---

### 4.2 Google Maps API 整合

💬 **命令範例**：
```
請實作 Google Maps 整合 (US-003, US-004):

功能:
1. 地址自動完成 (Autocomplete)
2. 地理編碼 (Geocoding) - 地址 → 經緯度
3. 地圖標記 (Marker) - 顯示房源位置
4. 地圖搜尋 (Bounds Search) - 搜尋特定區域房源

請使用 @googlemaps/js-api-loader
```

---

### 4.3 Stripe Payment 整合

💬 **命令範例**：
```
請實作完整的 Stripe 付款流程:

前端:
1. Stripe Elements (CardElement)
2. Payment Intent 創建
3. 3D Secure 處理

後端:
1. Payment Intent API
2. Webhook 處理 (payment_intent.succeeded)
3. 退款邏輯 (Refund API)

請包含測試卡號使用說明。
```

---

### 4.4 SendGrid Email 整合

💬 **命令範例**：
```
請實作 SendGrid Email 通知:

Email 類型:
1. 訂單確認 (給房客)
2. 新訂單通知 (給房東)
3. 取消通知 (雙方)
4. 評價提醒

請使用 Dynamic Template，
並提供 Template 參數定義。
```

---

## 📁 第五部分：最終專案結構

```
BnBBooking/
├── AISDLC/                           # 符號連結
├── CLAUDE.md
│
├── frontend/                         # Next.js 14
│   ├── app/
│   ├── components/
│   ├── lib/
│   ├── store/
│   ├── types/
│   ├── hooks/
│   └── package.json
│
├── backend/                          # Spring Boot 3.2
│   ├── src/main/java/com/bnbbooking/
│   │   ├── controller/
│   │   ├── service/
│   │   ├── repository/
│   │   ├── entity/
│   │   ├── dto/
│   │   ├── security/
│   │   └── config/
│   ├── src/main/resources/
│   │   └── application.yml
│   └── pom.xml
│
├── infrastructure/
│   ├── terraform/
│   ├── docker/
│   └── scripts/
│
└── docs/
    ├── requirements/
    │   └── FRD_BnBBooking_v1.0.md
    ├── design/
    │   ├── SRD_BnBBooking_v1.0.md
    │   ├── Database_Schema.md
    │   ├── Integration_Architecture.md
    │   └── Frontend_Backend_Interaction.md
    ├── api/ (28 個 API 規格)
    ├── tests/
    ├── reports/
    └── deployment/
```

---

## 🔍 常見問題 (FAQ)

### Q1: Stripe Webhook 在本地開發如何測試？

**A**: 使用 Stripe CLI
```bash
# 安裝 Stripe CLI
brew install stripe/stripe-cli/stripe

# 登入
stripe login

# 轉發 Webhook 到本地
stripe listen --forward-to localhost:8080/api/v1/payments/stripe/webhook

# 觸發測試事件
stripe trigger payment_intent.succeeded
```

---

### Q2: Google Maps API Key 如何限制使用？

**A**: 在 Google Cloud Console 設定:
1. API 限制: 只啟用 Maps JavaScript API, Geocoding API, Places API
2. 應用程式限制: 設定允許的網域 (例: bnbbooking.com)
3. 配額: 設定每日請求上限

---

### Q3: Firebase Auth 如何與後端 JWT 整合？

**A**: 流程:
```
1. 前端: 使用 Firebase Auth 登入 → 取得 Firebase ID Token
2. 前端: 將 ID Token 傳給後端 /api/v1/auth/firebase/callback
3. 後端: 驗證 Firebase ID Token (使用 Firebase Admin SDK)
4. 後端: 創建使用者 (如不存在) → 生成自己的 JWT
5. 前端: 儲存 Backend JWT，後續 API 使用此 Token
```

---

### Q4: PostgreSQL 資料庫遷移如何管理？

**A**: 使用 Flyway
```xml
<!-- pom.xml -->
<dependency>
    <groupId>org.flywaydb</groupId>
    <artifactId>flyway-core</artifactId>
</dependency>
```

遷移腳本放在: `backend/src/main/resources/db/migration/`
- V1__initial_schema.sql
- V2__add_user_preferences.sql

---

## ✅ 結論

透過本範例，您已學會:

1. ✅ Monorepo 專案結構設定 (前後端分離)
2. ✅ 使用 AISDLC Integration SOP 開發民宿預訂平台
3. ✅ 整合 4 個第三方服務 (Firebase, Stripe, Google Maps, SendGrid)
4. ✅ 前後端互動設計與 API 規格生成
5. ✅ Google Cloud 部署配置

**核心價值**: AISDLC 確保第三方整合的每個環節都有明確文檔、測試和驗證機制，避免整合時的常見問題 (API Key 洩漏、Webhook 未驗證、錯誤處理缺失)。

**下一步建議**:
1. 實際執行本範例命令
2. 閱讀 [Integration SOP](../../scenarios/integration/SOP.md)
3. 參考 [API 設計指南](../../guides/system/api/API_Versioning_Guide.md)

---

**文檔版本**: v1.0
**適用 AISDLC 版本**: v0.09
**最後更新**: 2025-12-16
**作者**: AISDLC 框架維護團隊
