# AISDLC 詳細實戰：舊專案維護 民宿網站

**專案類型**: Brownfield (既有系統改造)
**開發環境**: Cursor AI + Claude Code
**前端技術**: React (Next.js 14) + TypeScript
**後端技術**: Spring Boot 3.x + Java 17
**資料庫**: PostgreSQL 15
**部署**: Google Cloud Platform
**適用版本**: AISDLC v0.09+
**最後更新**: 2025-12-15

---

## 📋 目錄

1. [第一步：Cursor AI 專案路徑設定](#第一步cursor-ai-專案路徑設定)
2. [第二步：AISDLC 框架安裝](#第二步aisdlc-框架安裝)
3. [第三步：Claude Code 完整維護流程](#第三步claude-code-完整維護流程)
4. [附錄：命令速查表](#附錄命令速查表)

---

## 第一步：Cursor AI 專案路徑設定

### 1.1 既有專案結構

**專案位置**: `~/Projects/BnBWebsite/`

```
BnBWebsite/
├── frontend/                      # Next.js 前端
│   ├── src/
│   │   ├── app/                   # App Router (Next.js 14)
│   │   ├── components/            # React 組件
│   │   ├── lib/                   # 工具函式
│   │   └── styles/
│   ├── public/
│   ├── package.json
│   └── next.config.js
│
├── backend/                       # Spring Boot 後端
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/bnb/
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   ├── repository/
│   │   │   │   ├── model/
│   │   │   │   └── BnbApplication.java
│   │   │   └── resources/
│   │   │       ├── application.yml
│   │   │       └── db/migration/  # Flyway
│   │   └── test/
│   ├── pom.xml
│   └── Dockerfile
│
├── database/
│   ├── schema.sql
│   └── init-data.sql
│
└── deployment/
    ├── docker-compose.yml
    └── gcp/
        └── cloudbuild.yaml
```

### 1.2 建立 AISDLC 輔助目錄

```bash
cd ~/Projects/BnBWebsite

mkdir -p Docs/analysis           # 需求分析
mkdir -p Docs/planning           # 重構規劃
mkdir -p Docs/planning/api       # API 規格
mkdir -p Docs/reports            # 驗證報告
mkdir -p Docs/legacy             # 舊系統文檔

ls -la
```

**完整結構**:
```
BnBWebsite/
├── frontend/              # Next.js (既有)
├── backend/              # Spring Boot (既有)
├── database/             # PostgreSQL (既有)
├── deployment/           # GCP (既有)
├── Docs/                 # 📄 AISDLC 文檔
│   ├── analysis/
│   ├── planning/
│   ├── reports/
│   └── legacy/
└── AISDLC_v0.09/        # 🔴 步驟 2 安裝
```

### 1.3 開啟 Cursor AI

1. 打開 Cursor 應用程式
2. `File` → `Open Folder...`
3. 選擇：`~/Projects/BnBWebsite`
4. 點擊「Open」

**驗證**: Cursor 左側顯示 `frontend/`, `backend/`, `Docs/`

---

## 第二步：AISDLC 框架安裝

### 2.1 符號連結（推薦）

```bash
cd ~/Projects/BnBWebsite
ln -s ~/Cursor_Project/AISDLC_ALL/AISDLC_v0.09 ./AISDLC_v0.09
ls -la AISDLC_v0.09/AISDLC_INIT.md
```

### 2.2 驗證安裝

```bash
test -f AISDLC_v0.09/scenarios/brownfield/SOP.md && echo "✅ 安裝成功"
```

---

## 第三步：Claude Code 完整維護流程

### 階段 0: 啟動 Claude Code

按 `Cmd + Shift + P` → `Claude Code` → 開啟

---

### 階段 1: 專案評估

#### 命令 1.1: 載入 Brownfield SOP

```
請載入 AISDLC_v0.09/AISDLC_INIT.md

我要維護既有的民宿預訂網站（前後端分離架構），請使用 Brownfield SOP。

專案背景：
- 前端：Next.js 12 (Pages Router) → 需升級至 Next.js 14 (App Router)
- 後端：Spring Boot 2.7 → 需升級至 3.2
- 資料庫：PostgreSQL 13 → 需升級至 15
- 部署：Google App Engine → 需遷移至 Cloud Run

維護目標：
1. 技術棧升級（降低技術債）
2. 效能優化（首頁載入速度 >3s）
3. 新功能開發（即時聊天、動態定價）
4. SEO 優化

請啟動 Brownfield 階段 1：專案評估。
```

#### Claude Code 回應：

```
✅ 已載入 AISDLC v0.09 + Brownfield SOP

【階段 1: 專案評估】

🔴 人工確認點 1: 詳細評估

Q1: 專案規模？
- 前端程式碼行數
- 後端 API 端點數量
- 資料庫表格數量
- 月活躍用戶數

Q2: 技術債務？
- 測試覆蓋率（前端/後端）
- 已知 Bug 數量
- 技術文檔完整度

Q3: 效能問題？
- 首頁載入時間
- API 平均回應時間
- 資料庫查詢效能

Q4: 維護時程？
- 預計完成時間
- 是否可停機維護

請逐一回答。
```

#### 命令 1.2: 回答評估問題

```
Q1: 專案規模
- 前端：約 25,000 行 TypeScript/JSX
- 後端：45 個 API 端點，約 18,000 行 Java
- 資料庫：28 張表
- 月活躍用戶：約 5,000 人

Q2: 技術債務
- 前端測試：0%（無測試）
- 後端測試：35%（僅 Service 層）
- 已知 Bug：12 個（中低優先級）
- 技術文檔：僅簡單 README

Q3: 效能問題
- 首頁載入：3.2 秒（目標 <1.5s）
- API 回應：平均 450ms（可接受）
- DB 查詢：部分 N+1 問題

Q4: 時程
- 預計 12 週（3 個月）
- 可分階段上線，無法停機
- 需零停機部署

請產出評估報告與重構策略。
```

#### Claude Code 產出：

```
✅ 專案評估完成

【專案健康度評分】

整體：⚠️ 中等偏低（65/100）
- 功能完整度：85/100 ✅
- 程式碼品質：60/100 ⚠️
- 測試覆蓋率：20/100 🔴
- 效能表現：55/100 ⚠️
- 文檔完整度：40/100 🔴

【技術債務優先級】

🔴 高優先級：
1. 無前端測試（風險極高）
2. Next.js 12 已過時（安全性問題）
3. 首頁效能問題（影響 SEO）

🟡 中優先級：
4. Spring Boot 2.7 將停止支援
5. 資料庫 N+1 查詢
6. 技術文檔缺失

🟢 低優先級：
7. 中低優先級 Bug
8. 部署架構優化

【重構策略（12 週計畫）】

**階段 1 (週 1-3): 基礎設施升級**
- Next.js 12 → 14 (App Router)
- Spring Boot 2.7 → 3.2
- PostgreSQL 13 → 15
- 建立測試基礎設施

**階段 2 (週 4-6): 效能優化**
- 首頁 SSR/ISR 優化
- 圖片 CDN 遷移
- 解決 N+1 查詢
- Redis 快取層

**階段 3 (週 7-9): 新功能開發**
- 即時聊天（WebSocket）
- 動態定價引擎
- SEO 優化

**階段 4 (週 10-12): 測試與部署**
- 單元測試補充（目標 70%）
- E2E 測試
- 遷移至 Cloud Run
- 效能監控

📄 已創建: Docs/legacy/Project_Assessment_Report.md
📄 已創建: Docs/planning/Refactoring_Strategy_12Week.md

請確認策略，進入需求分析。
```

---

### 階段 2: 需求分析（舊系統文檔化 + 新需求）

#### 命令 2.1: 分析既有功能

```
請使用 brownfield-system-analysis workflow 分析既有系統：

分析範圍：
1. 前端路由結構 (frontend/src/pages/)
2. 後端 API (backend/src/main/java/com/bnb/controller/)
3. 資料庫 Schema (database/schema.sql)

產出：
- 既有功能清單
- API 端點文檔
- 資料模型 ERD
- 使用者旅程圖
```

#### Claude Code 產出：

```
✅ 既有系統分析完成

【既有功能清單 (15 個主要功能)】

**房源管理模組**
- F-LEGACY-001: 房源列表瀏覽（含篩選）
- F-LEGACY-002: 房源詳細頁
- F-LEGACY-003: 房源搜尋（地點、日期、人數）
- F-LEGACY-004: 房源收藏

**預訂管理模組**
- F-LEGACY-005: 日期選擇與價格計算
- F-LEGACY-006: 訂單建立
- F-LEGACY-007: 訂單查詢
- F-LEGACY-008: 訂單取消

**使用者管理模組**
- F-LEGACY-009: 註冊/登入（Email + Google OAuth）
- F-LEGACY-010: 個人資料管理
- F-LEGACY-011: 評價系統

**房東功能模組**
- F-LEGACY-012: 房源上架
- F-LEGACY-013: 訂單管理後台
- F-LEGACY-014: 收益統計

**其他功能**
- F-LEGACY-015: 客服表單

【API 端點文檔 (45 個端點)】

```java
// 主要 API 分類

1. 房源 API (12 個)
   GET    /api/v1/properties              // 列表查詢
   GET    /api/v1/properties/{id}         // 詳細資料
   POST   /api/v1/properties/search       // 進階搜尋
   GET    /api/v1/properties/{id}/availability  // 可訂日期
   ...

2. 預訂 API (10 個)
   POST   /api/v1/bookings               // 建立訂單
   GET    /api/v1/bookings/{id}          // 查詢訂單
   PUT    /api/v1/bookings/{id}/cancel   // 取消訂單
   GET    /api/v1/bookings/user/{userId} // 使用者訂單列表
   ...

3. 使用者 API (8 個)
   POST   /api/v1/auth/register          // 註冊
   POST   /api/v1/auth/login             // 登入
   GET    /api/v1/users/profile          // 個人資料
   ...

4. 房東 API (10 個)
   POST   /api/v1/host/properties        // 上架房源
   GET    /api/v1/host/bookings          // 訂單管理
   ...

5. 評價 API (5 個)
   POST   /api/v1/reviews                // 新增評價
   GET    /api/v1/reviews/{propertyId}   // 查詢評價
   ...
```

【資料庫 Schema (28 張表)】

核心表格：
- `properties` (房源)
- `bookings` (訂單)
- `users` (使用者)
- `reviews` (評價)
- `property_images` (房源圖片)
- `availability_calendar` (可訂日曆)

⚠️ 發現問題：
1. 缺少索引（查詢效能差）
2. 無軟刪除機制
3. 圖片儲存在資料庫（應改 CDN）

📄 已創建: Docs/legacy/Existing_API_Documentation.md
📄 已創建: Docs/legacy/Database_ERD.md
📄 已創建: Docs/legacy/User_Journey_Map.md

請確認新需求，產出 PRD/FRD。
```

#### 命令 2.2: 整合新需求

```
基於既有功能，請整合新需求並產出 PRD/FRD：

新需求：
1. 即時聊天功能
   - 房客與房東即時訊息
   - 訊息已讀狀態
   - 圖片分享
   - WebSocket 實作

2. 動態定價引擎
   - 依淡旺季調整價格
   - 依預訂率調整
   - 特殊節日加價
   - 早鳥優惠

3. 效能優化
   - 首頁 SSR + ISR
   - 圖片 CDN (Google Cloud Storage)
   - Redis 快取
   - 資料庫索引優化

4. SEO 強化
   - 動態 Meta Tags
   - Sitemap 自動生成
   - 結構化資料（JSON-LD）

保留功能：所有既有 15 個功能

產出：PRD、FRD、功能對照表
```

#### Claude Code 產出：

```
✅ 需求分析完成

【功能對照表】

| 功能 ID | 功能名稱 | 狀態 | 技術升級 |
|---------|---------|------|---------|
| F-001 | 房源列表 | 🔄 保留+優化 | SSR + Redis |
| F-002 | 房源詳細 | 🔄 保留+優化 | ISR + CDN |
| F-003 | 房源搜尋 | 🔄 保留+優化 | Elasticsearch |
| F-004 | 房源收藏 | ✅ 保留 | 無變更 |
| F-005~015 | (其他功能) | 🔄 保留+遷移 | Next.js 14 |
| F-016 | **即時聊天** | 🆕 新功能 | WebSocket |
| F-017 | **動態定價** | 🆕 新功能 | 定價引擎 |
| F-018 | **SEO 優化** | 🆕 強化 | Next.js SEO |

📄 已創建: Docs/analysis/PRD_BnBWebsite_v2.0.md
📄 已創建: Docs/analysis/FRD_BnBWebsite_v2.0.md
📄 已創建: Docs/planning/Feature_Comparison_Matrix.md

進入架構設計階段。
```

---

### 階段 3: 架構設計（升級 + 新功能）

#### 命令 3.1: 設計新架構

```
請設計 v2.0 架構：

前端升級：
- Next.js 14 (App Router)
- TypeScript 5.x
- Tailwind CSS 3.x
- React Query (資料快取)
- Zustand (狀態管理)

後端升級：
- Spring Boot 3.2
- Java 17
- Spring WebSocket (即時聊天)
- Spring Batch (定價引擎)
- Redis (快取層)

基礎設施：
- Google Cloud Storage (圖片)
- Cloud Run (部署)
- Cloud SQL (PostgreSQL 15)
- Memorystore (Redis)

請產出：
1. 新架構設計 (C4 Model)
2. 前後端目錄結構
3. 升級遷移計畫
4. API 規格更新
```

#### Claude Code 產出：

```
✅ 架構設計完成

【新前端架構 (Next.js 14 App Router)】

```
frontend/
├── src/
│   ├── app/                          # 🆕 App Router
│   │   ├── (marketing)/              # 路由群組
│   │   │   ├── page.tsx              # 首頁 (SSR)
│   │   │   ├── properties/
│   │   │   │   ├── page.tsx          # 房源列表 (ISR)
│   │   │   │   └── [id]/
│   │   │   │       └── page.tsx      # 房源詳細 (ISR)
│   │   │   └── layout.tsx
│   │   ├── (dashboard)/              # 會員區
│   │   │   ├── bookings/
│   │   │   ├── messages/             # 🆕 即時聊天
│   │   │   └── profile/
│   │   ├── (host)/                   # 房東後台
│   │   │   ├── properties/
│   │   │   ├── pricing/              # 🆕 動態定價
│   │   │   └── bookings/
│   │   ├── api/                      # API Routes
│   │   │   ├── auth/
│   │   │   └── webhooks/
│   │   ├── layout.tsx
│   │   └── not-found.tsx
│   │
│   ├── components/
│   │   ├── ui/                       # 基礎 UI
│   │   ├── features/                 # 功能組件
│   │   │   ├── property/
│   │   │   ├── booking/
│   │   │   ├── chat/                 # 🆕 聊天組件
│   │   │   └── pricing/              # 🆕 定價組件
│   │   └── layouts/
│   │
│   ├── lib/
│   │   ├── api/                      # API Client
│   │   ├── websocket/                # 🆕 WebSocket
│   │   ├── hooks/                    # React Hooks
│   │   ├── store/                    # Zustand Store
│   │   └── utils/
│   │
│   └── styles/
│       └── globals.css               # Tailwind
│
├── public/
├── package.json
└── next.config.js
```

【新後端架構 (Spring Boot 3.2)】

```
backend/
├── src/main/java/com/bnb/
│   ├── BnbApplication.java
│   │
│   ├── config/
│   │   ├── WebSocketConfig.java      # 🆕 WebSocket
│   │   ├── RedisConfig.java          # 🆕 Redis
│   │   ├── SecurityConfig.java
│   │   └── BatchConfig.java          # 🆕 定價 Batch
│   │
│   ├── controller/
│   │   ├── PropertyController.java
│   │   ├── BookingController.java
│   │   ├── MessageController.java    # 🆕 聊天 API
│   │   └── PricingController.java    # 🆕 定價 API
│   │
│   ├── service/
│   │   ├── PropertyService.java
│   │   ├── BookingService.java
│   │   ├── MessageService.java       # 🆕
│   │   ├── PricingEngineService.java # 🆕 定價引擎
│   │   └── CacheService.java         # 🆕 快取
│   │
│   ├── repository/
│   │   ├── PropertyRepository.java
│   │   ├── BookingRepository.java
│   │   ├── MessageRepository.java    # 🆕
│   │   └── PricingRuleRepository.java # 🆕
│   │
│   ├── model/
│   │   ├── entity/
│   │   ├── dto/
│   │   └── enums/
│   │
│   ├── websocket/                    # 🆕 WebSocket
│   │   ├── ChatHandler.java
│   │   └── MessageBroker.java
│   │
│   └── batch/                        # 🆕 定價 Batch
│       ├── PricingJob.java
│       └── PricingTasklet.java
│
└── resources/
    ├── application.yml
    └── db/migration/                 # Flyway
        ├── V1__initial_schema.sql
        └── V2__add_chat_tables.sql   # 🆕
```

【升級遷移計畫（12 週）】

**Week 1-3: 前端基礎升級**
```bash
# 步驟 1: 建立新分支
git checkout -b feature/nextjs-14-upgrade

# 步驟 2: 升級依賴
cd frontend
npm install next@14 react@18 typescript@5

# 步驟 3: 建立 App Router 結構
mkdir -p src/app/(marketing)
# 逐步遷移 pages/ → app/

# 步驟 4: 測試並部署
npm run build
npm run test
```

**Week 4-6: 後端升級 + Redis**
```xml
<!-- pom.xml 升級 -->
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.0</version>
</parent>

<!-- 新增 Redis -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>

<!-- 新增 WebSocket -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-websocket</artifactId>
</dependency>
```

**Week 7-9: 新功能開發**
- 即時聊天（WebSocket）
- 動態定價引擎
- SEO 優化

**Week 10-12: 測試 & Cloud Run 部署**

📄 已創建: Docs/planning/SRD_BnBWebsite_v2.0.md
📄 已創建: Docs/planning/Migration_Plan_12Week.md
📄 已創建: Docs/planning/Architecture_C4_Diagram.md

請確認，開始實作。
```

---

### 階段 4: 實作範例（Week 1-3: 前端升級）

#### 命令 4.1: Next.js 14 App Router 遷移

```
請協助遷移首頁至 Next.js 14 App Router：

任務：
1. 建立 app/page.tsx (SSR)
2. 整合 React Query
3. SEO 優化（Metadata API）
4. 效能優化（Image Optimization）

請提供完整程式碼。
```

#### Claude Code 產出：

```
✅ Next.js 14 App Router 遷移

【app/page.tsx】

```typescript
// src/app/page.tsx

import { Metadata } from 'next';
import { Suspense } from 'react';
import { FeaturedProperties } from '@/components/features/property/FeaturedProperties';
import { SearchForm } from '@/components/features/search/SearchForm';
import { Hero } from '@/components/layouts/Hero';

// 🆕 Next.js 14 Metadata API
export const metadata: Metadata = {
  title: '優質民宿預訂平台 | 您的旅行好夥伴',
  description: '探索全台精選民宿，享受溫馨住宿體驗。立即預訂您的理想住所！',
  openGraph: {
    title: '優質民宿預訂平台',
    description: '探索全台精選民宿',
    images: ['/og-image.jpg'],
  },
  // 🆕 JSON-LD 結構化資料
  other: {
    'application/ld+json': JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'WebSite',
      name: '優質民宿預訂平台',
      url: 'https://www.example.com',
    }),
  },
};

// 🆕 Server Component (SSR)
export default async function HomePage() {
  // Server-side data fetching
  const featuredProperties = await fetchFeaturedProperties();

  return (
    <main className="min-h-screen">
      {/* Hero Section */}
      <Hero />

      {/* Search Form */}
      <section className="container mx-auto py-12">
        <SearchForm />
      </section>

      {/* Featured Properties */}
      <section className="container mx-auto py-12">
        <h2 className="text-3xl font-bold mb-8">精選房源</h2>
        <Suspense fallback={<PropertyListSkeleton />}>
          <FeaturedProperties initialData={featuredProperties} />
        </Suspense>
      </section>
    </main>
  );
}

// Server Action for data fetching
async function fetchFeaturedProperties() {
  const res = await fetch('https://api.example.com/api/v1/properties/featured', {
    next: { revalidate: 3600 }, // 🆕 ISR: 1小時重新驗證
  });

  if (!res.ok) throw new Error('Failed to fetch');
  return res.json();
}
```

【FeaturedProperties.tsx (Client Component)】

```typescript
// src/components/features/property/FeaturedProperties.tsx

'use client';

import { useQuery } from '@tanstack/react-query';
import { PropertyCard } from './PropertyCard';
import type { Property } from '@/lib/types';

interface Props {
  initialData: Property[];
}

export function FeaturedProperties({ initialData }: Props) {
  // 🆕 React Query for client-side updates
  const { data: properties } = useQuery({
    queryKey: ['featured-properties'],
    queryFn: fetchFeaturedProperties,
    initialData,
    staleTime: 5 * 60 * 1000, // 5 分鐘
  });

  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
      {properties.map((property) => (
        <PropertyCard key={property.id} property={property} />
      ))}
    </div>
  );
}
```

【PropertyCard.tsx】

```typescript
// src/components/features/property/PropertyCard.tsx

'use client';

import Image from 'next/image';
import Link from 'next/link';
import { Heart } from 'lucide-react';
import type { Property } from '@/lib/types';

interface Props {
  property: Property;
}

export function PropertyCard({ property }: Props) {
  return (
    <Link href={`/properties/${property.id}`}>
      <div className="group relative rounded-lg overflow-hidden shadow-lg hover:shadow-xl transition">
        {/* 🆕 Next.js Image Optimization */}
        <Image
          src={property.imageUrl}
          alt={property.name}
          width={400}
          height={300}
          className="w-full h-64 object-cover group-hover:scale-105 transition"
          priority={false}
          loading="lazy"
        />

        {/* Favorite Button */}
        <button className="absolute top-4 right-4 p-2 bg-white rounded-full">
          <Heart className="w-5 h-5" />
        </button>

        {/* Property Info */}
        <div className="p-4">
          <h3 className="font-semibold text-lg">{property.name}</h3>
          <p className="text-gray-600">{property.location}</p>
          <p className="mt-2 font-bold">NT$ {property.pricePerNight} / 晚</p>
        </div>
      </div>
    </Link>
  );
}
```

✅ 首頁遷移完成

測試：
```bash
cd frontend
npm run dev
# 開啟 http://localhost:3000
```

效能檢查：
- ✅ SSR (Server-Side Rendering)
- ✅ ISR (Incremental Static Regeneration)
- ✅ Image Optimization
- ✅ SEO Metadata
- ✅ React Query 快取

接下來要實作哪個功能？
```

---

### 階段 5: 即時聊天功能（WebSocket）

#### 命令 5.1: 實作即時聊天

```
請實作即時聊天功能：

前端：
1. WebSocket 連接管理
2. ChatRoom 組件
3. 訊息列表 & 輸入框

後端：
1. WebSocket Configuration
2. MessageController
3. Message Entity

請提供完整程式碼。
```

#### Claude Code 產出（部分）：

```
✅ 即時聊天實作

【後端: WebSocketConfig.java】

```java
// backend/src/main/java/com/bnb/config/WebSocketConfig.java

@Configuration
@EnableWebSocketMessageBroker
public class WebSocketConfig implements WebSocketMessageBrokerConfigurer {

    @Override
    public void configureMessageBroker(MessageBrokerRegistry config) {
        config.enableSimpleBroker("/topic", "/queue");
        config.setApplicationDestinationPrefixes("/app");
    }

    @Override
    public void registerStompEndpoints(StompEndpointRegistry registry) {
        registry.addEndpoint("/ws")
                .setAllowedOrigins("http://localhost:3000")
                .withSockJS();
    }
}
```

【前端: useWebSocket.ts】

```typescript
// frontend/src/lib/websocket/useWebSocket.ts

import { useEffect, useRef, useState } from 'react';
import { Client } from '@stomp/stompjs';
import SockJS from 'sockjs-client';

export function useWebSocket(chatRoomId: string) {
  const clientRef = useRef<Client | null>(null);
  const [messages, setMessages] = useState<Message[]>([]);
  const [connected, setConnected] = useState(false);

  useEffect(() => {
    const client = new Client({
      webSocketFactory: () => new SockJS('http://localhost:8080/ws'),
      onConnect: () => {
        setConnected(true);

        // 訂閱聊天室
        client.subscribe(`/topic/chat/${chatRoomId}`, (message) => {
          const newMessage = JSON.parse(message.body);
          setMessages((prev) => [...prev, newMessage]);
        });
      },
    });

    client.activate();
    clientRef.current = client;

    return () => {
      client.deactivate();
    };
  }, [chatRoomId]);

  const sendMessage = (content: string) => {
    if (clientRef.current && connected) {
      clientRef.current.publish({
        destination: `/app/chat/${chatRoomId}`,
        body: JSON.stringify({ content }),
      });
    }
  };

  return { messages, connected, sendMessage };
}
```

(後續省略...)
```

---

## 附錄：命令速查表

### Brownfield Workflow

| Workflow | 命令 |
|----------|------|
| 專案評估 | `請使用 brownfield-system-analysis workflow` |
| 需求分析 | `請使用 requirements-validation-and-documentation workflow` |
| API 設計 | `請使用 api-specification-generation workflow` |
| 一致性檢查 | `請執行 document-consistency-check workflow` |

### 技術棧升級時程

- **Week 1-3**: Next.js 14 + TypeScript 5
- **Week 4-6**: Spring Boot 3.2 + Redis
- **Week 7-9**: WebSocket + 動態定價
- **Week 10-12**: Cloud Run 部署 + 測試

---

**更新**: 2025-12-15 | **版本**: v0.09
