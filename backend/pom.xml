<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/>
    </parent>

    <groupId>com.nextkey</groupId>
    <artifactId>ecommerce</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>NextKey E-Commerce</name>
    <description>B2B2C E-Commerce Platform - Phase 1</description>

    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.5</jjwt.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>10.13.0</version>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Utilities -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>

        <!-- MinIO S3 Client -->
        <dependency>
            <groupId>io.minio</groupId>
            <artifactId>minio</artifactId>
            <version>8.5.9</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- REST Assured for API E2E Testing -->
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>5.4.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>spring-mock-mvc</artifactId>
            <version>5.4.0</version>
            <scope>test</scope>
        </dependency>
        <!-- Stripe Payment SDK (Phase 3 - 真實 SDK 整合) -->
        <dependency>
            <groupId>com.stripe</groupId>
            <artifactId>stripe-java</artifactId>
            <version>24.3.0</version>
        </dependency>
        <!-- WireMock for Stripe HTTP API 測試（standalone 內建 Jetty，避免版本衝突） -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>3.5.4</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <!-- Java 21 -->
                    <release>21</release>
                    <!-- 啟用所有警告 -->
                    <compilerArgs>
                        <arg>-Xlint:all</arg>
                    </compilerArgs>
                    <!-- 註解處理器路徑 -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>1.18.32</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>3.3.0</version>
                <!-- plugin-level config: used by direct `mvn checkstyle:check` invocations (e.g., pre-commit hook) -->
                <configuration>
                    <configLocation>${project.basedir}/src/checkstyle/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <failOnViolation>true</failOnViolation>
                    <suppressionsLocation>${project.basedir}/src/checkstyle/checkstyle-suppressions.xml</suppressionsLocation>
                </configuration>
                <executions>
                    <!-- 主程式碼：完整規則，違規即失敗 -->
                    <execution>
                        <id>checkstyle-main</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <configLocation>${project.basedir}/src/checkstyle/checkstyle.xml</configLocation>
                            <consoleOutput>true</consoleOutput>
                            <failsOnError>true</failsOnError>
                            <failOnViolation>true</failOnViolation>
                            <suppressionsLocation>${project.basedir}/src/checkstyle/checkstyle-suppressions.xml</suppressionsLocation>
                            <sourceDirectories>
                                <sourceDirectory>${project.build.sourceDirectory}</sourceDirectory>
                            </sourceDirectories>
                        </configuration>
                    </execution>
                    <!-- 測試程式碼：僅檢查 UnusedImports，避免測試慣用模式的誤報 -->
                    <execution>
                        <id>checkstyle-test</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <configLocation>${project.basedir}/src/checkstyle/checkstyle-test.xml</configLocation>
                            <consoleOutput>true</consoleOutput>
                            <failsOnError>true</failsOnError>
                            <failOnViolation>true</failOnViolation>
                            <sourceDirectories>
                                <sourceDirectory>${project.build.testSourceDirectory}</sourceDirectory>
                            </sourceDirectories>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>3.22.0</version>
                <configuration>
                    <rulesets>
                        <ruleset>${project.basedir}/src/pmd/pmd-ruleset.xml</ruleset>
                    </rulesets>
                    <failOnViolation>true</failOnViolation>
                    <printFailingErrors>true</printFailingErrors>
                    <includeTests>true</includeTests>
                    <excludeRoots>
                        <excludeRoot>target/generated-sources</excludeRoot>
                    </excludeRoots>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- AISDLC v0.09: 排除 E2E 與 Integration 測試於 unit 階段
                 E2E 測試需要真實 HTTP/DB 連線，必須在 integration-test profile 跑
                 Integration 測試需要 Spring Context 與 DB，亦應於 integration 階段跑 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/*E2ETest.java</exclude>
                        <exclude>**/*E2E.java</exclude>
                        <exclude>**/*IntegrationTest.java</exclude>
                        <exclude>**/*TrueIntegrationTest.java</exclude>
                    </excludes>
                    <!-- reuseForks=true：整個測試套件共用一個 JVM。省下的不只是每類一次 JVM
                         冷啟動——整合測試的 Spring context 啟動在冷 JVM 裡要 31 秒，在暖 JVM 裡
                         只要 5～8 秒（類別載入／JIT 只付一次），那才是耗時的主導項。
                         此設定原為 false，理由寫「avoid SecurityContext pollution」；Sprint 104
                         查證後推翻：所有測試都自己清 SecurityContext，且實測在完全不裝清理機制下
                         跑 reuseForks=true，404 個整合測試零失敗——汙染顧慮不成立。
                         ThreadLocalIsolationExtension 因此是**護欄而非修復**：共用 JVM 之後
                         「不得殘留 ThreadLocal」成為永久不變量，交給一個全域擴充保證，
                         而不是仰賴每個新測試都記得清（見其 javadoc 與註冊驗證測試）。
                         runOrder=alphabetical 主要為了決定性——filesystem 順序在 macOS 與 Linux
                         runner 上不同，會讓本機與雲端的快取行為和順序相依 flaky 不一致；
                         次要才是讓同 context 設定的類別相鄰以提高快取命中。
                         Spring context 快取上限見 src/test/resources/spring.properties。 -->
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <runOrder>alphabetical</runOrder>
                </configuration>
            </plugin>
            <!-- failsafe 跑 integration 階段（由 -Pintegration-test 觸發）-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <includes>
                        <include>**/*E2ETest.java</include>
                        <include>**/*E2E.java</include>
                        <include>**/*IntegrationTest.java</include>
                        <include>**/*TrueIntegrationTest.java</include>
                    </includes>
                    <!-- reuseForks=true：整個測試套件共用一個 JVM。省下的不只是每類一次 JVM
                         冷啟動——整合測試的 Spring context 啟動在冷 JVM 裡要 31 秒，在暖 JVM 裡
                         只要 5～8 秒（類別載入／JIT 只付一次），那才是耗時的主導項。
                         此設定原為 false，理由寫「avoid SecurityContext pollution」；Sprint 104
                         查證後推翻：所有測試都自己清 SecurityContext，且實測在完全不裝清理機制下
                         跑 reuseForks=true，404 個整合測試零失敗——汙染顧慮不成立。
                         ThreadLocalIsolationExtension 因此是**護欄而非修復**：共用 JVM 之後
                         「不得殘留 ThreadLocal」成為永久不變量，交給一個全域擴充保證，
                         而不是仰賴每個新測試都記得清（見其 javadoc 與註冊驗證測試）。
                         runOrder=alphabetical 主要為了決定性——filesystem 順序在 macOS 與 Linux
                         runner 上不同，會讓本機與雲端的快取行為和順序相依 flaky 不一致；
                         次要才是讓同 context 設定的類別相鄰以提高快取命中。
                         Spring context 快取上限見 src/test/resources/spring.properties。 -->
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <runOrder>alphabetical</runOrder>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- 只在 CI 的整合測試 job 啟用（mvn verify -DskipUnitTests=true）。
         該 job 跑 mvn verify，而 verify 含 test 階段，不跳過就會把 backend-unit job
         已經跑完的同一批單元測試原封不動再跑一次（雲端實測 4m25s，零額外覆蓋）。

         刻意做成 profile，而不是在 surefire 的 <configuration> 直接寫
         <skipTests>${skipUnitTests}</skipTests>：POM 裡的顯式值會**蓋過命令列的
         -DskipTests**，使 `mvn package -DskipTests` 失去作用。Sprint 104 第一次
         就是這樣寫的，結果打斷了 validate-schema 的 JAR 建置（該階段無 test DB，
         Spring context 載入失敗），也會讓 workflow 的 Package 步驟變成第三次跑單元測試。
         profile 只在明確傳入 -DskipUnitTests=true 時才生效，預設行為完全不變。 -->
    <profiles>
        <profile>
            <id>skip-unit-tests</id>
            <activation>
                <property>
                    <name>skipUnitTests</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <!-- 只跳 surefire；failsafe 有自己的 skip 參數，不受影響 -->
                            <skipTests>true</skipTests>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
